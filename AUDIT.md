# 監査のための資料

このファイルは、外部監査を受けるための入口です。**このリポジトリだけを読めば監査に必要な情報が揃う**ようにしてあります。

RepoGloss は、GitHub の画面に出てくる英語のうち Git / GitHub 固有の用語に印を付け、日本語の説明を出す Chrome 拡張です。作者は非エンジニアで、実装は AI エージェントとの共同作業によります。**そのため、外部監査に合格するまでウェブストアへ提出しない**という運用にしています。

> このファイルと `docs/audit/` は**配布物には入りません**（[`scripts/package-files.mjs`](scripts/package-files.mjs) が配布物の唯一の正本）。監査用の資料であり、拡張の動作には関係しません。

---

## 1. 監査対象

| 項目 | 値 |
|---|---|
| **監査対象** | **v1.8.21 候補（未コミットの作業ツリー）**。commit / push / tag / Release / ストア提出はいずれも未実施 |
| 直前の版 | `v1.8.20` = コミット `e9be0713df082c84692bd0e700925f3fef6c25c7`（**第22回監査の指摘により提出しない**） |
| 基点コミット | `e9be0713df082c84692bd0e700925f3fef6c25c7`（ここからの差分が今回の変更） |
| 現在の既定ブランチ | `main` |
| Manifest | v1.8.21 / Manifest V3 |
| **最低の Chrome** | **105**（`minimum_chrome_version`。v1.8.6 で追加） |
| Chrome API 権限 | `storage` のみ |
| サイトアクセス | `https://github.com/*` |
| `host_permissions` | 宣言なし |
| 実行されるコード | [`src/matcher.js`](src/matcher.js) と [`src/content.js`](src/content.js) の2本（同梱のみ・リモートコードなし） |
| 外部依存パッケージ | **なし**（`package.json` は検証用の scripts だけ。ZIP 生成も Node 標準の `zlib` で自作） |
| ストア公開中の版 | **v1.7.1**（v1.8.0〜v1.8.21 はいずれも未提出） |

差分は次のコマンドで確認できます。

```sh
git diff --name-only v1.8.20             # 直前の版との差分（作業ツリー）
# CHANGED:   manifest.json src/content.js styles.css README.md DESIGN.md
# UNCHANGED: src/matcher.js locales/dict.json PRIVACY.md LICENSE icons/icon16.png icons/icon32.png icons/icon48.png icons/icon128.png
# → ほかに scripts/ と tests/ と docs/（配布物には入らない）
```

> **この2行は手で書きません。** `verify` が `git diff` の実測と突き合わせ、**両方に載っている
> ファイルがあれば落とします**（第21回 RG-21-08。実測: v1.8.19 の同じ説明が `styles.css` を
> 「変わっている」と「無変更」の両方に挙げていた——版を上げるときに片方だけ直したため）。

**この版はまだ作業ツリーにあります。commit も PR もタグも作っていません。**

> **監査対象と `main` の関係について（第11回 RG-11-06 の是正）。** 前回まで、監査対象を「`main` の先頭」と**相対的に**書いていました。提出候補の SHA を記録するコミットを1つ積んだ時点で、その記述は事実でなくなります（実際にそうなりました）。いまは対象をタグとコミットだけで名乗り、**現在の `main` の SHA はここに書きません**——書けば、その値もまた次のコミットで古くなるためです。両者の関係は文章ではなく検査で担保します。`state` が `uncommitted` でないとき、`verify` が「ここが名乗るタグと、いまの配布13ファイルが同じか」を `git diff` で突き合わせ、タグを引けない環境では**通しません**。

### 1-1. 提出候補

**提出候補は、`main` の CI が作った成果物です。** 手元で作ったものは `--release` 検査で落ちるようにしてあり、提出するのは常に CI が出したものだけです。

現在版の事実を、機械で読める形で1か所にまとめます（**ここが正本**。他の節はここを引用します）。

```yaml
version:            1.8.21
state:              uncommitted        # commit / push / tag / Release / ストア提出はいずれも未実施
base_commit:        e9be0713df082c84692bd0e700925f3fef6c25c7
commit:             null
tag:                null
candidate_zip:      null
candidate_bytes:    null
candidate_sha256:   null
content_sha256:     null
combined_sha256:    null
workflow_run:       null
superseded:
  - version: 1.8.20
    tag: v1.8.20
    commit: e9be0713df082c84692bd0e700925f3fef6c25c7
    zip: repogloss-1.8.20.zip
    sha256: cc024072ac641c990684edbcdc040d941e6d71e89d47ef71f74a23bcdff54b9d
    reason: 第22回監査の指摘により提出しない
  - version: 1.8.19
    tag: v1.8.19
    commit: d76a6cdb68d5ab5e0fac5895934010f66e585295
    zip: repogloss-1.8.19.zip
    sha256: 19b5ed38fef1b4f2913740b96f10375201d3c7c5344188f99f7bc801d05db8d6
    reason: 第21回監査の指摘により提出しない
  - version: 1.8.18
    tag: v1.8.18
    commit: 617779bced04f86cc47046e187bdd437b79340f8
    zip: repogloss-1.8.18.zip
    sha256: b66312dc1538d61c4bdd1d40b1c3491eac40a8583b0b6b78cbcf77d6a8df76d7
    reason: 第20回監査の指摘により提出しない
  - version: 1.8.17
    tag: v1.8.17
    commit: 9ae28b907e0ec1b662f360b28fa82c040e0a82e1
    zip: repogloss-1.8.17.zip
    sha256: ae30290f714bbca47bc710cf8abe80e0aa169a11b486425437d26cdf34e47f2c
    reason: 第19回監査の指摘により提出しない
  - version: 1.8.16
    tag: v1.8.16
    commit: 879fc0fbccb17c63b154ff0dc96f6cd8a04f772b
    zip: repogloss-1.8.16.zip
    sha256: df4f2cff7217001857e7804af0a70a17afb26628944e0e07cc7b9f998c1cb338
    reason: 第18回監査の指摘により提出しない
  - version: 1.8.15
    tag: v1.8.15
    commit: f62fedcad4eeb2590c8158b7fe152aac530702ba
    zip: repogloss-1.8.15.zip
    sha256: 73880071fdf214d6e58f167417f4bf3dfef3c6aa7c0089f72761d9ca371da9dd
    reason: 第17回監査の指摘により提出しない
  - version: 1.8.14
    tag: v1.8.14
    commit: fe2f3f82a34900d018af15023796f9b9b6a59c34
    zip: repogloss-1.8.14.zip
    sha256: 16ae5d29d520a52cc17160c411c2b49587db044242f3952cb72d9518f9616c7a
    reason: 第16回監査の指摘により提出しない
  - version: 1.8.13
    tag: v1.8.13
    commit: 54f98991b3bf3838012484e4a9c4ce1462fab8d5
    zip: repogloss-1.8.13.zip
    sha256: f8ad0962f151b7c3bea3922c16bfcf8d821add065c0f926e2951b9dd022eefbf
    reason: 第15回監査の指摘により提出しない
  - version: 1.8.12
    tag: v1.8.12
    commit: 5afa0e284a199377902067df9c3cb582ec9b2051
    zip: repogloss-1.8.12.zip
    sha256: a107c2119ccc1c3b31b32699300d5ea73a1e85549339418ad31d543fb75018bd
    reason: 第14回監査の指摘により提出しない
  - version: 1.8.11
    tag: v1.8.11
    commit: 92bd593e35839b448f97b051ab427e7a1384c398
    zip: repogloss-1.8.11.zip
    sha256: a3da61d5083900da8e535e6ec346b6d8f76f85841339b090c2e03a5a4933ea3f
    reason: 第13回監査の指摘により提出しない
  - version: 1.8.10
    tag: v1.8.10
    commit: aaa770d687ed5861785f629e494662823ba7d875
    zip: repogloss-1.8.10.zip
    sha256: c194a9cbabd6496fc97d056b5713fcc2036e258745b43cfae29ecc874ce32fce
    reason: 第12回監査の指摘により提出しない
  - version: 1.8.9
    tag: v1.8.9
    commit: cea7a29590747f110d6cda68c162d339499c5fdf
    zip: repogloss-1.8.9.zip
    sha256: 4f1f3166c8ee35b76786f6062307a96a6ea25772fc0d51dfdb06a9623d70112f
    reason: 第11回監査の指摘により提出しない
  - version: 1.8.8
    tag: v1.8.8
    commit: 6c1351a5b98539a61e854310503fd92ad59a7d24
    zip: repogloss-1.8.8.zip
    sha256: d3a2786ba0f87ac51a75bcb6e9186a6ae3e86ea93ef804905da7fefebcf9bfac
    reason: 第10回監査の指摘により提出しない
  - version: 1.8.7
    tag: v1.8.7
    commit: 308bf038efe18f857bf5bb655bce65b991712a93
    zip: repogloss-1.8.7.zip
    sha256: 9f590f7c299d8c5f4bd62fc9cf87a5a9a2c8215945683faca76d0cf496afb93e
    reason: 第9回監査の指摘により提出しない
  - version: 1.8.6
    tag: v1.8.6
    commit: 3848b89a9cdc7df62dc2eaff8c4ae4b6e6e18086
    zip: repogloss-1.8.6.zip
    sha256: 6603ca7a2f7653f3786b197b4058bfc45c354a8c5692dcdd5e3cf2455018ba35
    reason: 第8回監査の指摘により提出しない
store_published:    1.7.1
```

取り下げた版のタグと ZIP は、すべて保存したまま動かしていません（`v1.8.20` を打った前後で、既存24タグ＝48 ref が1文字も動いていないことを `refs/tags` 一覧の突き合わせで確認済み。1件の改変なら検出できることも、同じ実行で対照として示しています）。**直前の `v1.8.19`（`19b5ed38…`）は、第21回監査の指摘により提出しません。**

**提出候補は、タグの中身と1バイトも違いません。** CI の run `31879198285` の `release-zip` が出した成果物を落として展開し、13ファイルを `git show v1.8.20:<path>` と突き合わせて **13/13 一致**（対照の `v1.8.19` とは5ファイルで相違）。`--release` 検査にも合格しています（実測: `EXIT=0`）。

**ZIP は誰でも再現できます。** 日時を 1980-01-01 に固定し、並び順を配布一覧の順に固定してあるため、同じ内容からは1バイト違わない同じものができます。配布13ファイルの合算ハッシュは **ubuntu / macos / windows / 手元 macOS の4者で完全に一致**しています（`32a66c1c…`）。

> **⚠️ ZIP のハッシュだけでは、CI の成果物と手元ビルドを区別できません。** ビルドが決定的なので、同じ内容からは同じ ZIP ができます（今巡も実測: 手元ビルドと CI の成果物は**バイト単位で同一**の `cc024072…`）。区別が付くのは **sidecar だけ**です——検査したのは `builtInCI: true` / `workflowRunId: 31879198285` を持つほうです。手元ビルドの sidecar を正本に据えると、同じ ZIP でも `--release` 検査は落ちます（実測: `EXIT=1`・「提出候補は CI で作ったものに限る（builtInCI が true でない）」）。

```sh
npm run package:zip -- --allow-uncommitted   # commit 前に試す場合。名前に UNCOMMITTED が入る
npm run package:verify-zip                   # 中身と身元の記録を検査する
```

---

## 2. 今回（v1.8.21）で直したこと

第22回監査の指摘 **P1 3件・P2 3件**への対応（**6件すべて**）と、その確認中に自分で見つけた**見た目の不具合1件**。詳細と証拠は [`docs/audit/v1.8.21-changes.md`](docs/audit/v1.8.21-changes.md)。

| ID | 内容 | 対応 |
|---|---|---|
| RG-22-01（P1） | 退役した印の、closed shadow root の中の button を失効させていなかった | 退役と同じ場所で `deactivateShadowIcon` を呼ぶ。押せなくしてから button と説明文を取り除き、所有の記録も消す。**`<slot>` は残す**ので、ページが入れた本文は描かれ続ける |
| RG-22-02（P1） | ページ由来の custom property が shadow の中まで継承し、印が 0 画素になった | 実際の塗りを測り、塗れないときだけ **shadow の中のインライン指定**で塗り直す（ページからは触れない）。印の実測も、host ではなく**押せる実体**を見る |
| RG-22-03（P1） | `pointer-events: none` の覆いが当たり判定に映らず、0 画素の印が残った | 覆いの候補を「`pointer-events: none` を宣言している規則の選択子」と「`style` 属性」から引き、**点ごとに**塞がれているかを見る。断定できる覆い（地が不透明・透けていない・位置指定あり・重なり順で前）だけを数える |
| RG-22-04（P2） | CSSOM の宣言だけの書き換えに気づかず、2秒ごとの確認まで遅れた | **控えの見直しを 400ms ごとに回す**（使いすぎの門つき）。指紋へ宣言の中身を混ぜる案は、実測で 10,000規則あたり 23〜31ms かかるため採らなかった（→ §4-1b） |
| RG-22-05（P2） | `:target` と、同じ相手のままの `:focus-visible` を合図にしていなかった | `hashchange` / `popstate`（あれば Navigation API）と `keydown` を購読し、**入力のやり方**（マウス／キーボード）を擬似クラスの鍵に入れる |
| RG-22-06（P2） | ページが合成した click だけで、恒久の ON/OFF 設定を書き換えられた | 切替ボタンの click で `event.isTrusted` を検査する。**この門は設定を変える経路だけ**に掛け、受け身の合図（カーソル・フォーカス・スクロール）へは広げない |
| （自分で発見） | **v1.8.20 の印は、輪が二重で "i" が箱の外へはみ出していた** | shadow へ移したとき light DOM 側の絵をそのまま残していたため。single ring に戻し、"i" を shadow の中の button へ移した（→ §4-1 の画像） |

**6件すべて、直す前に v1.8.20 の実物で再現しました。**

> **⚠️ 見た目の不具合は、監査も私も見つけていませんでした。** 画素の数と意味づけだけを測っていて、**印を拡大して見ることをしていなかった**ためです。RG-22-02 を直す途中で 8 倍に拡大して撮影し、初めて気づきました。

## 3. キーボード到達性の不変条件

```text
入口（trigger）として使ってよいのは、次をすべて満たす要素だけ:
  1. el.isConnected
  2. input[type=hidden] でない
  3. el.matches(':disabled') でない   （fieldset[disabled] の継承と、最初の legend の例外を含む）
  4. el.closest('[inert]') が無い
  5. el.tabIndex >= 0                 （ブラウザが解釈した後の値を使う）
  6. getComputedStyle が display:none / visibility:hidden|collapse でない
  7. el.getClientRects().length > 0   （display:contents はここで落ちる）

  ※ 6 と 7 は「その要素自身」について必ず確認する。先祖・子孫から推し量らない。
  ※ label は自身を入口にせず、label.control を 1〜7 で判定する。
  ※ tabIndex < 0 は、role や容器が何であっても入口にしない。
```

入口が見つからなければ、その語には注記しません（`skip`）。**`aria-hidden` な印だけを残すことはしません。** 見送った語は、同じページの後方のふつうの文章で説明されます。

---

## 4. 検証の結果

### 4-1. 手元（macOS 26 / Google Chrome 151.0.7922.76 / Node.js v22.22.3・2026-08-15）

**この版はまだ commit していないので、CI の結果はありません**（§4-2）。下は手元での実行結果です。

| command | exit | 結果 |
|---|---:|---|
| `node --check src/matcher.js` / `src/content.js` | 0 | 構文 OK |
| `npm test` | 0 | 単体 **63件**全成功 / 構成検査 **379項目**・不一致0（辞書61語・version 1.8.21） |
| `npm run test:e2e` | 0 | **415件**全成功（v1.8.20 は 383件。**+32件**） |
| `npm run package:stage` / `:verify` | 0 | 13ファイル一致 |

**6件すべて、直す前に v1.8.20 の実物で再現しました。** 印の画素は実際のスクリーンショットから数え、押せる解説の数は CDP のアクセシビリティのツリーから読み、Tab は実際に押しています。

| ID | 再現（v1.8.20 の実物） |
|---|---|
| RG-22-01 | ページが退役した印を本文の入れ物に使い回すと、「「branch」の解説」という押せる button が**2個**残り、その節点で**実際に Tab が止まった**（停止点1）。ページの本文は保たれていた |
| RG-22-02 | ページが `--fgColor-accent: transparent` を置くと、印の 196画素のうち白でない画素が **0**（対照は 129）。それでも Tab の停止点として残り、後方の読める同じ語は説明されなかった |
| RG-22-03 | 不透明な箱を `pointer-events: none` で印の上へ置くと、画素が **129 → 0**。5点すべての `elementFromPoint` は**印**を返し、印は正規のまま残った |
| RG-22-04 | 選択子も規則の数も変えずに `cssRules[0].style.display` を `none` → `inline` にすると、650ms 後も印**0**（2秒ごとの確認でようやく1） |
| RG-22-05 | `:target` で開いたメニューは 600ms 後も印**0**／マウスで押した相手のままキーを1回押して `:focus-visible` にしても 600ms 後は印**0**（どちらも2秒後に1） |
| RG-22-06 | ページ側の script から `.click()` を1回呼ぶと「解説 ON」→「解説 OFF」。**別のタブを開いてもそのまま OFF**（＝保存まで届いていた）。合成 `MouseEvent` でも切り替わった |

### 4-1a. 直したあと（同じ見本・対照つき）

| ID | 実測（v1.8.21） |
|---|---|
| RG-22-01 | 押せる解説 **1個**・使い回された節点の Tab 停止点 **0**・`PAGE CONTENT` は表示されたまま。外して戻しても生き返らない |
| RG-22-02 | 印の白でない画素 **104 / 196**。⚠️ **対照（変数をいじらない側）も 104** ——§4-1c で印の絵を1本の輪へ直したので、透明な変数を置かれても**見た目が変わらなくなった** |
| RG-22-03 | 不透明な覆いで前方 **0**・後方 **1**・Tab の停止点 **0**。同じ画面の矩形の画素は **0**（覆う前は 104）。⚠️ 対照3つ（透明な覆い・部分的な覆い・ふつうのスクロール枠）はどれも落とさない |
| RG-22-04 | 宣言を書き換えてから **600ms 以内**に印が付く |
| RG-22-05 | `:target`・`:focus-visible` とも **600ms 以内**に印が付く |
| RG-22-06 | `.click()` と合成 `MouseEvent` では**表示も保存も変わらない**。⚠️ 対照＝本物のマウス click・Enter・Space では切り替わる |

### 4-1c. 監査が挙げていない、自分で見つけた不具合

**v1.8.20 の単独の印は、輪が二重で "i" が箱の外へはみ出していました。** RG-22-02 を直す途中で印を **8倍に拡大して撮影**して気づきました。

- light DOM の host が、自分の枠（14×14）と `::after` の "i" を**まだ描いていた**
- shadow の中の button が、**もう1つの輪**（8×11）を描いていた（`.78em` が二重に掛かって縮んでいた）
- host は `display: inline-flex` なので、`::after` の "i" が2つ目の項目として**右へ押し出され**、箱の外へ出ていた

**画素の数と意味づけだけを測っていて、拡大して見ることをしていませんでした。** 直したうえで、「light DOM の host は枠も "i" も描かない」「button が host と同じ大きさ」を E2E へ固定しています。

> **⚠️ 直しの途中で、私が5回間違えました。** ①周期の見直しで `dropStates()` を呼び、次のカーソルの合図が全件を回し直すようにしてしまった（`checkVisibility` が 3,542 → 18,108 回）。②その見直しで**まとめ直しを予約**し、「1回のページ変更で、まとめ直しは1回」を守っている検査を壊した（1 のはずが 3）。③間隔を件数に関わらず一定にして、5,000候補で 35,042 → 46,844 回に増やした。④shadow の中の button を `border-color: transparent` で隠したが、**幅が残る**ので 12×12 の輪が 14×14 の箱に描かれた。⑤直った側の試験で、**もう居ない印**の画素を測ろうとして落ちた（覆ったら印は退役するので、同じ**矩形**を測らなければならない）。①②③は**性能の対照**が、④⑤は**自分で書いた受入試験**が捕まえました。

### 4-1b. 性能（2026-08-15・同じ見本で v1.8.20 と並べて実測）

計測器は**隔離された世界の中**、カーソルは**本物のマウス**、陽性対照つき。控えは `latentPage(n)` に `:hover` 規則を1つ足したもの。

**カーソルを10回動かしたとき**の `checkVisibility` 呼び出し回数:

| 控えの候補数 | v1.8.20 | v1.8.21 |
|---:|---:|---:|
| 500 | 3,542 | 7,554 |
| 2,000 | 14,042 | 15,894 |
| 5,000 | 35,042 | **15,018** |

**何もしないで4秒待ったとき**:

| 控えの候補数 | v1.8.20 | v1.8.21 |
|---:|---:|---:|
| 500 | 2,016 | 6,028 |
| 2,000 | 8,016 | 8,014 |
| 5,000 | 20,016 | 15,012 |

**実時間**（同じ待ち時間に対する経過。小さいほど画面が詰まっていない）:

| 場面 | v1.8.20 | v1.8.21 |
|---|---:|---:|
| カーソル・5,000候補 | 6,043ms | **4,131ms** |
| 待つだけ・5,000候補 | 5,299ms | **4,009ms** |

- 控えが少ないときは**測り直す回数が増えます**。「合図の出ない変化を 2秒でなく 600ms で拾う」ことの代償です。
- 控えが多いときは、**間隔を空ける**のと使いすぎの門が効いて v1.8.20 より少なくなります。
- 1秒あたりに見る候補数は **1,250件で頭打ち**なので、ページが大きくなっても増え続けません。
- 実時間はどの場面でも v1.8.20 より短くなりました。

### 4-2. CI

**この版はまだ commit していないので、CI は走っていません。** 直前の版（`v1.8.20` = `e9be0713…`）の CI は run [`31879198285`](https://github.com/Driedsandwich/repogloss/actions/runs/31879198285) で8ジョブすべて success でした（`verify` 358項目・E2E 383件・3 OS のハッシュ一致・提出候補 ZIP `cc024072…`）。**その版は第22回監査の指摘により提出しません。**

---

## 6. 権限・通信・保存データ（v1.8.20 → v1.8.21 の差分）

| 項目 | v1.8.20 | v1.8.21 | 差分 |
|---|---|---|---|
| `permissions` | `["storage"]` | 同じ | **なし** |
| `host_permissions` | 宣言なし | 同じ | **なし** |
| サイトアクセス | `https://github.com/*` | 同じ | **なし** |
| `content_scripts` | `src/matcher.js`, `src/content.js` | 同じ | **なし** |
| `web_accessible_resources` | `locales/dict.json`, `styles.css` | 同じ | **なし** |
| 外部通信 | なし | なし | **なし** |
| 保存するもの | `iiyakuEnabled`（真偽値1つ） | 同じ | **なし** |
| 辞書 | 61語 | 61語 | **なし** |

**`manifest.json` の差分は `version` の1行だけ**（実測）。`locales/dict.json`・`src/matcher.js`・アイコン・`LICENSE`・`PRIVACY.md` は無変更です。

配布13ファイルのうち変わったのは **manifest.json・src/content.js・styles.css・README.md・DESIGN.md** の5つです。

> **保存するものは増えていません。** 第22回 RG-22-06 で足したのは「ページが合成した click では設定を変えない」という門だけで、保存する項目も、保存する場所（`chrome.storage.local`）も変わっていません。

**`PRIVACY.md` の記載は manifest と機械で突き合わせています**（第20回 RG-20-10）。`web_accessible_resources` を足し引きしたのに日英の表が古いままなら、`verify` が落ちます。

---

## 7. 意図的に直していない既知の制約

「まだ確認していないこと」を「確認済み」と書かないための一覧です。

| # | 内容 | 理由 |
|---|---|---|
| 1 | **実スクリーンリーダー（NVDA / VoiceOver）未確認** | 環境がない。DOM とキーボードの E2E は実施しているが、実際の読み上げとは別物として扱い、成功扱いにしない |
| 2 | **Windows 実機の目視未確認** | CI で windows-latest の E2E は通っているが、**字形やレイアウトの見た目は確認していない**。CI の成功と目視確認は別のものとして区別する |
| 3 | **200% / 400% ズーム、高コントラスト系テーマ未確認** | 同上 |
| 4 | **`MutationObserver` が属性の変化だけでは再走査しない** | 見張っているのはノードの追加と URL の変化。要素が `display:none` や `inert` へ**変わっただけ**では、その場では追随しない。ただし**そのあと新しいノードが追加されれば、そこで使えなくなった古い印は片づけて付け直す**（3-2-3 の `usableGloss`）。第5回監査が必須とした「不可視化のあとに可視の候補が現れた場合」は直してある。属性変化だけの即時追従は、費用に見合わないため見送る |
| 4-b | **テキストノードの中身だけが差し替わる更新を追わない**（`characterData`） | 実害が再現していないため据え置き（前回監査の方針どおり） |
| 5 | **Developer Dashboard の実文言未読** | 提出者のログインが必要な画面。§5 のとおり転記欄を用意した |
| 6 | 実サイト計測は macOS の headless Chrome 151・幅1600px・4ページのみ | 他の画面幅やログイン状態では結果が変わりうる |
| 7 | ZIP の検査は自作の読み書き実装に依存 | 緩和として、Python の `zipfile` と `unzip -t`（いずれも無関係な実装）でも読めることを確認している |
| 8 | **ZIP のバイト再現性は、確認した Node/zlib の版でのみ実測** | deflate の出力は zlib の版に依存しうる。将来の任意の版で同一とは主張しない。作った版は成果物の `*.zip.json`（`environment.node` / `environment.zlib`）へ記録している |
| 9 | **1px＋`clip` 以外の視覚非表示は網羅していない** | 実サイトで確認できた形（1px 四方＋`clip`／`clip-path`）だけを狭く判定している。正規の小さな UI を巻き込まないことを優先した。「視覚的に隠された領域をすべて除外する」とは主張しない |
| 10 | **3D の変形（`matrix3d` / `perspective`）が掛かった場所では、切り抜きの形を解かない**（v1.8.15） | 2D の回転・拡大縮小・傾きは、形を変形前の座標へ戻して当てる。3D は断定できないので可視へ倒す |
| 11 | **スクロールで動かせる向きを判別しない**（v1.8.15） | 書字方向（LTR / RTL）で符号が変わるため、両側へ同じだけ広く見積もる。読める側へ倒すための意図的な過大評価 |
| 12 | **カーソルの合図の費用が、控えの候補数に比例したまま**（v1.8.15） | 1件あたりの費用は下げた（実測 50〜75% 減）。範囲を「カーソルの下の枝」へ狭めるのは、`:has()` や兄弟結合子があるため安全でない |
| 13 | ~~**レイヤー名は、偶然ぶつからない綴りにしてあるだけ**~~ → **解消（v1.8.18）** | **見た目を走り出しに読み込む形へ作り替え、レイヤー名も選択子の合言葉も読み込みごとに変えた**（第19回 RG-19-06）。ページが同じ名前のレイヤーを先に宣言しても、ページ自身の指定は無傷（実測: レイヤー無し／レイヤーだけ／順序を逆に宣言、の3通りとも `display:grid` / 赤 / 140×30px / `z-index:5` のまま）。3巡持ち越していた件 |
| 14 | **カーソルの費用は、控えの候補数に比例したまま**（v1.8.16） | 見直す範囲を狭めるのは `:has()` があるため安全でない。使ってよい時間に上限（2秒あたり200ms）を置いて頭打ちにした。上限は「減らす仕組み」ではない |
| 15 | **見分けられない複製には触れない**（v1.8.17 で v1.8.16 の割り切りを撤回） | 形だけで複製と決めつけると**ページ所有の要素を壊す**（第18回 RG-18-06 で実測）。名札も合言葉も全部消された複製は、幅0で押せるだけの要素として残る。**その形の停止点はページが自分だけでも作れる**。構造として断つには印を closed shadow root の中へ入れる作り直しが要る |
| 16 | **`mask` の合成（Porter-Duff）を解かない**（v1.8.17） | 足し合わせ以外は「断定できない」とし、その候補では語を使い切らない（まとめ直しの最後に、まだどこも引き受けていない語だけを引き受け直す） |
| 17 | **変形前の矩形が、ちょうど45°など解の定まらない角度では復元できない**（v1.8.17） | そのときは平行四辺形で広く見積もる（読める側へ倒す） |
| 18 | **背景の抜きは `background-size: auto` の場合だけ塗る場所を出す**（v1.8.17） | それ以外は届く側へ倒す |
| 19 | **カーソルの費用は、`:hover` を使う規則のあるページでは、またいだ回数に比例したまま**（v1.8.20 でさらに緩和） | **変わった範囲**（動く前と後のいちばん近い共通の祖先）の中だけを測り直す（第21回 RG-21-07）。実測: 2,000候補・10移動で `checkVisibility` **24,048 → 12,036回**（−50%。500件 −58%・5,000件 −58%）。⚠️ `:has()` や兄弟結合子を使う規則があるページ、stylesheet が変わった回は絞らない。**2秒ごとの確認は必ず全体を進める**ので、比例そのものは残る |
| 20 | ~~**Windows の E2E が、まれに Chrome の起動待ちで落ちる**~~ → **是正（v1.8.18）** | **起動段階のこのコマンドだけ**、診断（試行回数・経過・失敗の内容・プロセスの状態・CDP の待ち・stderr）を残して1回に限り試し直す（第19回 RG-19-09）。置き場所は呼ぶ側ではなく CDP の送信側（呼び出しは 44 か所ある）。**製品の assertion の失敗は試し直さない**（`STARTUP_METHODS` は `Extensions.loadUnpacked` の1つだけ）。`continue-on-error` は使わない |
| 21 | ~~**見分けられない複製が、幅0の押せる点として残る**~~ → **是正（v1.8.20）** | 単独で押せる印を **closed shadow root の中の `<button>`** へ移した（第21回 RG-21-06。第19回から3巡持ち越し）。light DOM の host（`<span>`）は `role` も `tabindex` も持たないので、複製しても押せる点にならない。実測: 全署名を消した複製は shadow root を持たず（`cloneNode` は複製しない）、押せる要素0・Tab の停止点も増えない。⚠️ `<sup>` には `attachShadow` できない（`NotSupportedError`）ため host を `<span>` へ変えた。⚠️ shadow の中に `<slot>` を置いていないと、退役した印をページが本文の入れ物に使い回したときに**その文字が描かれない**（実測で再現）。⚠️ 装飾扱いの印（入口に紐づくもの）は従来どおり light DOM のまま——もともと `role` も `tabindex` も持たない |
| 22 | **`background-size: auto` の画像の自然寸法を解かない**（v1.8.18） | 同期では分からず、読みに行けば新しい通信になる。断定せず後回しにする（gradient の auto は解ける） |
| 23 | **吸い寄せ（scroll snap）の止まり位置そのものを解かない**（v1.8.18） | いま見えているかだけを断定し、あとはスクロールが落ち着いてから見直す |
| 24 | **SVG の `<mask>` を `match-source` で参照した場合の luminance を解かない**（v1.8.18） | `mask-mode` を明示した層だけを解く |
| 25 | **断定できない語には、印を付けない**（v1.8.19） | 第20回 RG-20-01。実際には見えているのに断定できない語（解けない `filter` の `url()`・ちょうど45°・未対応の `clip-path` の中など）は、ページに他の出現が無ければ**説明されない**。「見えない押せる点を作らない」ほうを取った |
| 26 | **SVG の `filter` の中身を解かない**（v1.8.19） | `url()` はすべて「断定できない」。透明な filter と可視な filter を区別できないので、**どちらにも印が付かない**（第20回監査の受入条件のうち1つは、この方針では満たせない） |
| 27 | **一様でない `mask` は断定しない**（v1.8.19） | gradient の値は場所で変わるので、要素全体の状態からは語の位置を決められない（第20回 RG-20-04） |
| 28 | **印の当たり判定は、いま見えている印にだけ当てる**（v1.8.19） | スクロールで送られた先にある印の座標で当たり判定をすると、そこに描かれている別のものが返る。覆いと呼ぶのは、印とも印の祖先とも関係のない要素が前に出ているときだけ |
| 29 | **URL の画像は、画素が分からないので断定しない**（第21回 RG-21-01） | 背景も `mask` も、gradient 以外の層は「断定できない」へ送る。⚠️ **不透明な画像で塗った／覆った語も説明されなくなった**（第17回 RG-17-04 と第19回 RG-19-04 の対照2件を、印を付けない側の期待値へ変えた）。`data:` の URL なら canvas でほどけるが、非同期の作り直しが要るので**次の候補**として残す |
| 30 | ~~**`pointer-events: none` の覆いは、当たり判定では見えない**~~ → **是正（v1.8.21）** | 覆いの候補を「`pointer-events: none` を宣言している規則の選択子」と「`style` 属性」から引き、**点ごとに**塞がれているかを見る（第22回 RG-22-03）。数えるのは断定できる覆いだけ——地が不透明・透けていない・位置指定あり・重なり順で印より前。実測: 不透明な覆いで前方0・後方1・Tab の停止点0、透明な覆い／部分的な覆い／ふつうのスクロール枠はどれも落とさない。⚠️ **IntersectionObserver v2 は使えない**（覆いが無くても `isVisible: false` を返す。同じ実行でふつうの要素は true）。⚠️ 全要素を歩く形も採らない（10,000要素で 6〜13ms）
| 31 | **`background-repeat` の `space` / `round` は解かない**（第21回 RG-21-02） | 隙間や縮尺が絡むので、その層は断定しない |
| 32 | **印自身が `pointer-events: none` にされたら、覆いを判定しない**（第22回 RG-22-03） | 当たり判定に何も映らないので、覆われているかを断定できない。**「印の見え方を完全に検証している」とは主張しない** |
| 33 | **覆いの候補は、読める stylesheet と `style` 属性からしか引かない**（第22回 RG-22-03） | 読めない stylesheet が1枚でもあれば全要素を歩くほうへ落とすが、ページ側の shadow DOM の中と、擬似要素で描かれた覆いは見ない。画像や gradient で塗った箱も「断定できない」＝覆いではない側へ倒す |
| 34 | **合図の出ない変化は、最短でも 400ms 遅れる**（第22回 RG-22-04） | 控えの見直しを周期で回して拾う。控えが多いページでは間隔を空ける（1秒あたり 1,250候補・最大 2,000ms）ので、そのぶん遅れる。⚠️ 指紋へ宣言の中身を混ぜる案は、実測（10,000規則で 23〜31ms）により採らなかった |
| 35 | **`pushState` だけの移動は、Navigation API が無い環境では拾えない**（第22回 RG-22-05） | `hashchange` / `popstate` は購読しているが、`pushState` は event を出さない。Navigation API があればそこで拾い、無ければ DOM の変更か周期の見直しに任せる |

---

## 8. これまでの監査の履歴（履歴）

| 回 | 指摘 | 結果 | 対象版 |
|---|---:|---|---|
| 1 | 15件 | 全件が実在。対応して v1.8.0 へ | v1.7.1 |
| 2 | 12件 | 全件が実在。対応して v1.8.1 へ（v1.8.0 は提出見送り） | v1.8.0 |
| 3 | 6件 | 全件が実在。対応して v1.8.2 へ（v1.8.1 は提出見送り） | v1.8.1 |
| 4 | 8件 | 全件が実在。対応して v1.8.3 へ（v1.8.2 は提出見送り） | v1.8.2 |
| 5 | 8件 | 全件が実在。対応して v1.8.4 へ（v1.8.3 は提出見送り） | v1.8.3 |
| 6 | 7件 | 全件が実在。対応して v1.8.5 へ（v1.8.4 は提出見送り） | v1.8.4 |
| 7 | 8件 | 全件が実在。対応して v1.8.6 へ（v1.8.5 は提出見送り） | v1.8.5 |
| 8 | 5件 | 全件が実在。対応して v1.8.7 へ（v1.8.6 は提出見送り） | v1.8.6 |
| 9 | 8件 | 全件が実在。対応して v1.8.8 へ | v1.8.7 |
| 10 | 7件 | 全件が実在。対応して v1.8.9 へ | v1.8.8 |
| 11 | 6件 | 全件が実在。対応して v1.8.10 へ | v1.8.9 |
| 12 | 6件 | 全件が実在。対応して v1.8.11 へ | v1.8.10 |
| 13 | 7件 | 全件が実在。対応して v1.8.12 へ | v1.8.11 |
| 14 | 9件 | 全件が実在。対応して v1.8.13 へ | v1.8.12 |
| 15 | 9件 | 全件が実在。対応して v1.8.14 へ | v1.8.13 |
| 16 | 10件 | 全件が実在。対応して v1.8.15 へ | v1.8.14 |
| 17 | 8件 | 全件が実在。対応して v1.8.16 へ | v1.8.15 |
| 18 | 8件 | 全件が実在。対応して v1.8.17 へ | v1.8.16 |
| 19 | 9件 | 全件が実在。対応して v1.8.18 へ | v1.8.17 |
| 20 | 10件 | 全件が実在。対応して v1.8.19 へ | v1.8.18 |
| 21 | 8件 | 全件が実在。対応して v1.8.20 へ | v1.8.19 |
| 22 | 6件 | 全件が実在。対応して v1.8.21 へ | v1.8.20 |
| **合計** | **180件** | **180件連続で、事実と違う指摘は0件** | |

> **⚠️ この表は、第21回まで8行しか無く、第9回以降が抜けていました**（v1.8.21 で補完）。
> 版を上げるたびに新しい行を足していなかったためで、第21回 RG-21-08 と同じ形の抜けです。
> 各回の件数は `docs/audit/v1.8.*-changes.md` の冒頭（「指摘は P1 n件・P2 m件」）と一致します。
> **合計が各行の和と一致すること**を `verify` が数えます。

第4回の指摘のうち1件（RG-4-07）は、**該当が11件ではなく12件**でした。辞書で `s` で終わるキーを数え直すと `request changes` も該当し、`request changeses` が作られていました。指摘そのものは正しく、影響範囲がわずかに広かったものです。

提出しない版のタグ・Release・履歴は、いずれも**そのまま残しています**（付け替えも削除もしていません）。

---

## 9. 監査で特に見てほしいところ

0. **「その語が読めるか」の判定に穴がないか**（3-2-3 / `isVisibleOccurrence`）。`checkVisibility` に任せた部分と、自分で形を見ている部分（1px＋`clip`）の切り分けは妥当か。正規の小さな UI を巻き込む恐れはないか。
0-b. **`usableGloss` の判定と、古い印を取り除く処理**。本文を壊す経路や、同じキーの印が2つ残る経路が無いか。
0-c. **`EXTRA_FORMS` に残した30語の綴り**。GitHub の画面に実在しない形や、普通の英単語と衝突する形が混ざっていないか。
1. **§3 の不変条件に穴がないか。** とくに「先祖から推し量らない」を徹底できているか、`resolvePlacement` の祖先たどりに抜けがないか。
2. **roving を全廃した判断の副作用。** GitHub 以外の画面構成や、将来 GitHub が変わった場合に、説明が届かなくなる語が出ないか。実サイト4ページでの「0件」という測定で十分か。
3. **E2E の oracle が本当に独立か。** テストが本番の判定式を言い換えているだけになっていないか。
4. **§5 のデータ申告が公式定義と整合するか。** 「ウェブサイトのコンテンツ」だけを選ぶ範囲設定が妥当か（過小申告になっていないか）。
5. **自作の ZIP 実装。** [`scripts/zip.mjs`](scripts/zip.mjs) の読み書きに、配布物を壊しうる欠陥がないか。決定性の担保は十分か。
6. **CI がゲートとして閉じているか。** `continue-on-error` を外した以外に、失敗が隠れる経路が残っていないか。

---

## 10. 主なファイルの案内

| 内容 | 場所 |
|---|---|
| 判定ロジック本体（DOM を触る部分） | [`src/content.js`](src/content.js) |
| 用語の判定（DOM を触らない・Node からも呼べる） | [`src/matcher.js`](src/matcher.js) |
| 辞書（61語） | [`locales/dict.json`](locales/dict.json) |
| 設計の意図と実測の記録 | [`DESIGN.md`](DESIGN.md) |
| プライバシーポリシー | [`PRIVACY.md`](PRIVACY.md) |
| 拡張として読み込む E2E | [`tests/e2e/`](tests/e2e/) |
| 用語判定の単体テスト | [`tests/matcher.test.js`](tests/matcher.test.js) |
| 配布物の検査（壊した ZIP を9種ぶつける） | [`tests/package.test.js`](tests/package.test.js) |
| 提出物の身元の schema と、その検査（28件） | [`scripts/provenance.mjs`](scripts/provenance.mjs) / [`tests/provenance.test.js`](tests/provenance.test.js) |
| 配布物の一覧（唯一の正本） | [`scripts/package-files.mjs`](scripts/package-files.mjs) |
| 構成と文書の整合検査（379項目） | [`scripts/verify.mjs`](scripts/verify.mjs) |
| ZIP の読み書き（自作） | [`scripts/zip.mjs`](scripts/zip.mjs) |
| ZIP の検査 | [`scripts/verify-zip.mjs`](scripts/verify-zip.mjs) |
| CI | [`.github/workflows/ci.yml`](.github/workflows/ci.yml) |
| ストア掲載情報と提出記録 | [`STORE_LISTING.md`](STORE_LISTING.md) |
| **今回の変更の詳細と証拠** | [`docs/audit/v1.8.21-changes.md`](docs/audit/v1.8.21-changes.md) |

---

## 11. 監査の前提

- **提出はこの監査に合格してから**行います（[`STORE_LISTING.md`](STORE_LISTING.md) §8-2-3 に条文化）。
- 指摘は、**事実の裏取りができる形**（該当ファイル・行・再現手順）でいただけると助かります。過去4回はすべて実物と一致していたため、こちらでも1件ずつコードで確認しています。
- 「直していないこと」を「直した」と書かないようにしています。§7 の一覧に漏れがあれば、それ自体を指摘してください。
