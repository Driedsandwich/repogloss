# 設計メモ

RepoGloss の語の選び方、判定の仕組み、既知の限界。README に入れると読者の判断材料が埋もれるため分けている。

---

## 1. なぜ 61 語しかないのか

v1.5 までは 151 語を収録していた。内訳を分類したところ、この拡張にしか出せない価値がある語は 56 語（37%）だった。残りを外した。

| 分類 | 数 | 例 | 外した理由 |
|---|---|---|---|
| 画面のラベル | 37 | `view all` `your profile` `set up in desktop` `suggested workflows` | GitHub が画面を変えると意味を失う。追随には継続的な更新が要る |
| 一般的な英単語 | 44 | `on` `off` `new` `open` `code` `save` `search` `title` | 英文のいたるところに出るため、無関係な箇所に印が付く |
| 一般的な IT 用語 | 10 | `api` `https` `markdown` `snapshot` | 英和辞書で足りる |
| 構造上マッチしない語 | 4 | `-u` `.gitignore` `CONTRIBUTING.md` `branch -M` | 単語境界の判定に引っかからず、もともと機能していなかった |

そのうえで、欠けていた概念語 5 語（`repository` `pull` `upstream` `collaborator` `force push`）を足して 61 語にした。`repository` は 45 語版にはあったが 151 語版で失われていた。

**測定**: `github.com/octocat/Hello-World` の表示テキストに対する検出数は 107 箇所 → 65 箇所（2026-08-02 実測）。減った分はすべて誤検出だった。

**この選び方の帰結**: 収録語は少ない。その代わり、GitHub が画面を変えても内容が古くならない。更新が止まったときに壊れる方向を選ばなかった、ということ。

## 2. 説明文の書き方

- 「何をするものか」＋「なぜそうするのか」を両方書く。言い換えだけで終わらせない
- 説明の中で `リポジトリ` `ブランチ` `マージ` などを噛み砕かずに使わない
- 1〜2 文、全角 60〜110 字程度
- 取り返しのつかない操作は、何を失うかを必ず書く
- 画面のラベルではなく**概念**を説明する

例（`reset`）:

> 作業の記録を、指定した時点まで巻き戻す操作です。やり方によっては、まだ記録に残していない編集内容が完全に消え、他の人と共有した後に巻き戻すと食い違いが起きるので、意味が分かるまで実行しないでください。

## 3. 判定の仕組み

- 辞書のキーを**長い順**に並べて正規表現を組む。正規表現の `|` は左から先に当たるため、`pull` が `pull request` より前にあると「Pull requests」に `pull` の説明が付く
- 単数形・複数形の揺れを吸収する（`-s` `-es` および `-y → -ies`）。GitHub の画面は複数形が多く、これが無いと最も目立つタブで当たらない
- 一つのテキストの中の**すべての一致**に印を付ける（後ろの一致から順に分割して挿入する）
- ただし**同じ語はページで最初の1回だけ**。説明は一度読めば足りるうえ、git の解説ページのような文書では印が数百個になり本文が読めなくなる（実測 843 → 43 個）。単複は同じ語として扱う（`Pull requests` と `pull request` で1回）
- 「付けた語」は Set ではなく **キー → 挿入した印の要素** の Map で持つ。GitHub がサイドバー等を描き直すと印ごと消えることがあり、記録だけが残ると二度と付かなくなるため。参照先が DOM から外れていたら付け直す
- GitHub は画面を読み直さずに差し替えることがある。**URL が変わったら画面全体を走査し直す**（URL の書き換えより先に内容が差し込まれることがあり、その分が飛ばされている可能性があるため）。ただし**「付けた語」の記録は空にしない**。GitHub はヘッダーやサイドバーを画面遷移をまたいで保持するので、そこに付いた印が残ったまま数え直すと本文に同じ語がもう一度付く。判定は「いま画面に印が生きているか」だけで足りる
- コード表示部分を除外する。`<pre>` `<code>` に加え、現行の React 版コードビューア（`.react-code-lines` `.cm-editor` など）も対象
- 辞書に当たらないテキストでは可視判定（`getComputedStyle`）を呼ばない。全テキストノードでレイアウト計算を強制しないため
- 印は文字コードの記号ではなく CSS で描く。`U+1F6C8`（🛈）は Windows のフォントにはあるが macOS には無く、Mac では豆腐になっていた
- ツールチップは自前で描く。`title` 属性は表示までに 1 秒前後の待ちがあり、拡張側から短くできない
- 判定の部分（正規化・正規表現の組み立て・単複の引き直し・重複の除外）は [`src/matcher.js`](./src/matcher.js) に分けてある。DOM を触らないので、ブラウザと Node の両方から同じコードを呼べる。判定の規則を変えると `tests/matcher.test.js` が落ちる

## 3-2. 印の読み上げとキーボード操作

印は文章の中にも、リンクやボタンの中にも入る。同じ扱いにすると片方が壊れるため、入った場所で分けている。

| 入った場所 | 読み上げ | キーボード |
|---|---|---|
| ふつうの文章の中 | 印自体に説明文を名前として与える（`role="img"` + `aria-label`） | 印が Tab の順路に入る |
| リンク・ボタンの中 | 印を装飾として扱う（`aria-hidden`） | 印は順路に入れない。**そのリンク自体**に止まったときに説明を出す |

リンクの中の印に名前を与えると、GitHub 本来のリンク名（「Pull requests」）の後ろへ解説文が丸ごと足され、読み上げが長くなる。リンクの中へ `tabindex` を足すと、操作できる要素の入れ子になる。どちらも避けるための分岐で、説明は表示中だけ `aria-describedby` で関連付ける（相手が元々持っていた `aria-describedby` は残して戻す）。

## 3-3. ON / OFF でページを読み直さない

v1.7.1 までは、切り替えのたびに `location.reload()` を呼んでいた。書きかけの Issue コメントが消え、スクロール位置も失われるため、状態の切り替えだけを行うようにした。

- OFF: 監視（MutationObserver）を止め、吹き出しを閉じ、`<html>` に付けたクラスで印を **CSS で隠す**
- ON: 隠すのをやめ、`document.body` を一度走査し、監視を再開する
- 印を DOM から**消さない**のは、消すと分割済みのテキストノードが元に戻らず、ON へ戻しても付き直さない語が出るため
- 保存に失敗したときは、表示を切り替える前の状態へ戻す（保存できていないのに見た目だけ変わる状態を残さない）
- `chrome.storage.onChanged` を購読しているので、片方のタブで切り替えると他の GitHub タブにも反映される

## 4. 辞書を増やす・直す

[`locales/dict.json`](./locales/dict.json) に `"英語キー": "日本語の説明"` を追記し、`chrome://extensions/` で更新を押す。

- キーは**小文字**で書く
- 単数形で書けば複数形（`-s` `-es` `-y → -ies`）は自動で拾う
- 空白を含むキー（`pull request` など）は、空白の数や種類が違っても一致する
- 先頭が記号のキー（`.gitignore` など）は単語境界の判定に引っかからないため、そのままでは機能しない

## 5. 既知の限界

- **継続的な使用による検証を経ていない。** 実機で確認したのは基本的な表示と動作まで
- 大きなページでの速度への影響が未確認
- 他の拡張との重なり: 英和辞典系の拡張が同じホバーで別のポップアップを出す場合、表示が重なる。z-index は最大値にしてこちらが前に出るようにしたが、相手も同値なら DOM 順で決まる
- Windows / Linux での表示が未確認
- 説明文が実際の初学者にとって分かりやすいかを検証していない
- 印は CSS で描いているため、`::after` の生成内容が本文のコピーに混ざらないはずだが、これは仕様からの推測で実測していない
- ツールチップは `document.body` を基準に絶対配置している。`body` に `transform` や `position: relative` が掛かっている環境では位置がずれる可能性がある（GitHub 上では未確認）
- `styles.css` の `.iiyaku-tooltip` は v1.5 の改修で使われなくなり、CSS だけが残っていた。v1.7.0 で自前ツールチップを戻したことで再び使われている
- **支援技術（スクリーンリーダー）での読み上げを実測していない。** 印の役割分けは実装しているが、実際の読み上げソフトで確かめたのは未了
- **DOM の監視をまとめていない。** MutationObserver の呼び出しごとに走査しており、画面遷移を検知した回は本体全体と追加分の両方を見る。重複した注記は起きないが、走査そのものの費用は残る。ただし 6-1-2 の実測では最初の走査が 10〜13ms で 50ms 超の処理も出ておらず、**まとめる改修を急ぐ理由は今のところ無い**（先に入れると v1.7.1 の画面遷移対応を壊しかねない）
- 既存の文字列の書き換え（`characterData`）や、`hidden` / `aria-hidden` の解除だけで表示された要素は捕捉していない

## 6. 検証

### 6-1. いま再現できる検証

clone した直後に `npm test`（依存パッケージなし）で実行できる。中身は次の3つで、GitHub Actions でも同じものを走らせている。

| 対象 | 実体 | 内容 |
|---|---|---|
| 構文 | `node --check` | `src/matcher.js` と `src/content.js` |
| 用語の判定 | [`tests/matcher.test.js`](./tests/matcher.test.js) | 全 61 キーの自己一致、大文字小文字と空白、単複（`-s` `-es` `-y→-ies`）、長いキーが短いキーに食われないこと、同じ語の重複除外、`fork/forks` のように単複が別キーの語、一致位置の正しさ、部分一致しないこと |
| 構成と文書 | [`scripts/verify.mjs`](./scripts/verify.mjs) | Manifest V3・権限が `storage` だけであること・対象サイトが `https://github.com/*` だけであること・参照ファイルの実在と配布一覧との一致・辞書の形式・辞書の語数と README/DESIGN の記載の一致・CSS クラス名の整合・外部通信や `eval` が無いこと |
| 拡張としての動作 | [`tests/e2e/extension.e2e.mjs`](./tests/e2e/extension.e2e.mjs)（`npm run test:e2e`） | 配布ファイルだけを並べて Chrome へ実際に読み込ませ、manifest が受理されること・印の表示・コード領域の除外・リンクの中の印が装飾扱いであること・キーボードと Escape・**ON/OFF でページが読み直されず書きかけが残ること**・**タブをまたいだ設定の同期** |

**前の3つが確かめるのは、判定の規則と構成の整合だけ**で、画面上の見え方・速度・読み上げは含まない。4つめの E2E は「拡張として読み込んだ状態」を作るので、`chrome.storage` の実挙動や読み込み順まで含む。

E2E の作り方の要点: `--load-extension` は現行 Chrome で無効化されているため、CDP の `Extensions.loadUnpacked` を使う（`--remote-debugging-pipe` と `--enable-unsafe-extension-debugging` が要る）。`github.com` は `--host-resolver-rules` でローカルの HTTPS サーバへ向けるので、外部通信もアカウントも要らない。**読み込むのは配布する13ファイルだけ**（[`scripts/package-files.mjs`](./scripts/package-files.mjs)）なので、出荷物に足りないファイルがあれば落ちる。

### 6-1-2. ブラウザでの実測（2026-08-05）

拡張として読み込む方法は現行の Chrome で塞がれているため、CDP（ブラウザの遠隔操作）でページへ content script を流し込み、`chrome.*` だけを肩代わりして測った。**この方法で分かるのは DOM 上の振る舞いまで**で、拡張として常用したときの体感は含まない。

**速度**（Chrome 151 headless・`k88hudson/git-flight-rules`・3〜5回）

| 版 | 最初の走査 | 走査したテキスト | 付けた印 | レイアウト計算 | 50ms 超の処理 |
|---|---:|---:|---:|---:|---|
| v1.7.1（変更前） | 10〜13 ms | 5,469 か所 | 37 | 526 回 | なし |
| 変更後 | 10〜13 ms | 5,469 か所 | 37 | 526 回 | なし |

**速度は変わっていない**（判定を `matcher.js` へ移しても、走査の量は同じ）。**「大きなページでの速度」を測ったのはこれが初めて**で、結果は 50ms のしきい値に対して十分小さい。DOM 監視のまとめ（バッチ化）を急ぐ理由は、この数字を見る限り無い。

> 測り方の注意: 最初は Node 側で CDP 呼び出しの往復時間を測っており、変更前 51ms・変更後 13ms という差が出た。ページの中で測り直すと両方 10〜13ms で、差は**計測方法の癖**だった。順序を入れ替えても同じ差が出たため、実物の差だと誤認しかけた。

**読み上げ属性**（同じ3ページ・リンクやボタンの中に入った印の数）

| ページ | 変更前：リンク名に解説文が足されていた印 | 変更後 |
|---|---:|---:|
| `octocat/Hello-World` | 19 / 20 | 0 |
| `k88hudson/git-flight-rules` | 32 / 41 | 0 |
| `Spoon-Knife`（コード表示） | 11 / 12 | 0 |

印の総数はどのページでも変更前と同じで、コード表示部分の印は両方とも 0 だった（＝注記の範囲は変えていない）。

### 6-2. 過去の記録

以下は当時その場で書いたスクリプトや実機で確認した記録で、**リポジトリには残っていない**（再実行はできない）。6-1 のテストは、このうち判定に関する部分を再現できる形で書き直したもの。

| 対象 | 方法 | 日付 |
|---|---|---|
| 用語の判定 | 13 ケースの自動テスト（`Pull requests` が `pull` でなく `pull request` と判定される／`repositories` が `repository` と判定される／`force push` が `push` に食われない／`squash merge` が `merge` に食われない／同一文中の複数語がすべて注記される 等） | 2026-08-02 |
| 辞書の自己一致 | 全 61 キーが、それ自身の文字列に対して正しく当たること | 2026-08-02 |
| 検出数の変化 | `github.com/octocat/Hello-World` の表示テキストで 107 → 65 箇所 | 2026-08-02 |
| 構成の整合 | `manifest.json` が参照する 4 ファイルの実在、JSON の妥当性、`node --check`、CSS クラス名の一致 | 2026-08-03 |
| 印の見た目 | ブラウザで描画してスクリーンショットを目視（12px / 16px / 28px とホバー時）。同じ画面で `U+1F6C8` が豆腐になることも再現 | 2026-08-03 |
| 実機での動作 | Chrome で `github.com/octocat/Hello-World` を開き、印31個の表示とツールチップの表示を確認。`Pull requests` に `pull request` の説明が付くことも確認 | 2026-08-03 |
| コード領域の除外 | `github.com/octocat/Spoon-Knife/blob/main/index.html` で、コード表示部分（`.react-code-lines`）の中の印が 0 件であることを確認。現行のコードビューアは `<pre>` `<code>` を使っておらず、効いているのは `.react-code-lines` の指定 | 2026-08-03 |
| 印の密度 | 実機で計測。`Hello-World` 31 個（うち重複9）、`k88hudson/git-flight-rules` 843 個（うち重複800）。同じ語を1回にすると 22 個 / 43 個になる | 2026-08-03 |
| 重複排除のロジック | 5 ケースの自動テスト（同一ノード内の重複／ノードをまたぐ重複／単複が同じ語に潰れる／`repositories` と `repository`／異なる語は両方付く） | 2026-08-03 |
| 重複排除の追随性 | 5 ケースの自動テスト（同一ノード内は1回／別ノードの2回目は付かない／印が消えたら付け直す／付け直した後は再び1回だけ／画面遷移後は付く） | 2026-08-03 |
| 画面遷移での重複 | 4 ケースの自動テスト（ヘッダーに付く／本文では既出の語に付かない／遷移後は消えた語だけ付き直す／ヘッダーが消えたら付き直す） | 2026-08-03 |
| 印の数（直接読み込み） | `git-flight-rules` で 43 個。本文に出現する辞書語 45 のうち、`ci` と `release` はより長いキー（`ci/cd`・`releases`）で既に付いており実質全数 | 2026-08-03 |
| 印の数（画面遷移で到達） | 実機で計測。`git-flight-rules` と Issues 一覧を往復した後で 43 個・重複 0 件。直接読み込みと一致 | 2026-08-03 |
