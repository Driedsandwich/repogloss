# 設計メモ

RepoGloss の語の選び方、判定の仕組み、既知の限界。README に入れると読者の判断材料が埋もれるため分けている。

---

## 1. なぜ 61 語しかないのか

v1.5 までは 151 語を収録していた。内訳を分類したところ、この拡張にしか出せない価値がある語は 56 語（37%）だった。残りを外した。

| 分類 | 数 | 例 | 外した理由 |
|---|---|---|---|
| 画面のラベル | 37 | `view all` `your profile` `set up in desktop` `suggested workflows` | GitHub が画面を変えると意味を失う。追随には継続的な更新が要る |
| 一般的な英単語 | 44 | `on` `off` `new` `open` `code` `save` `search` `title` | 英文のいたるところに出るため、無関係な箇所に印が付く |
| 一般的な IT 用語 | 10 | `api` `https` `markdown` `snapshot` | 英和辞書で足りる |
| 構造上マッチしない語 | 4 | `-u` `.gitignore` `CONTRIBUTING.md` `branch -M` | 単語境界の判定に引っかからず、もともと機能していなかった |

そのうえで、欠けていた概念語 5 語（`repository` `pull` `upstream` `collaborator` `force push`）を足して 61 語にした。`repository` は 45 語版にはあったが 151 語版で失われていた。

**測定**: `github.com/octocat/Hello-World` の表示テキストに対する検出数は 107 箇所 → 65 箇所（2026-08-02 実測）。減った分はすべて誤検出だった。

**この選び方の帰結**: 収録語は少ない。その代わり、GitHub が画面を変えても内容が古くならない。更新が止まったときに壊れる方向を選ばなかった、ということ。

## 2. 説明文の書き方

- 「何をするものか」＋「なぜそうするのか」を両方書く。言い換えだけで終わらせない
- 説明の中で `リポジトリ` `ブランチ` `マージ` などを噛み砕かずに使わない
- 1〜2 文、全角 60〜110 字程度
- 取り返しのつかない操作は、何を失うかを必ず書く
- 画面のラベルではなく**概念**を説明する

例（`reset`）:

> 作業の記録を、指定した時点まで巻き戻す操作です。やり方によっては、まだ記録に残していない編集内容が完全に消え、他の人と共有した後に巻き戻すと食い違いが起きるので、意味が分かるまで実行しないでください。

## 3. 判定の仕組み

- 辞書のキーを**長い順**に並べて正規表現を組む。正規表現の `|` は左から先に当たるため、`pull` が `pull request` より前にあると「Pull requests」に `pull` の説明が付く
- 受け付ける綴りは**生成しない**。必要な複数形だけを `src/matcher.js` の `EXTRA_FORMS` に明示して並べる。綴りの規則から機械的に作ると、`security` → `securities`（金融商品）、`ci` → `cis`、`main` → `mains` のように、意味の違う普通の英単語まで拾ってしまう（v1.8.3 で実際に拾っていた）
- 一つのテキストの中の**すべての一致**に印を付ける（後ろの一致から順に分割して挿入する）
- ただし**同じ語はページで最初の1回だけ**。説明は一度読めば足りるうえ、git の解説ページのような文書では印が数百個になり本文が読めなくなる（実測 843 → 43 個）。単複は同じ語として扱う（`Pull requests` と `pull request` で1回）
- 「付けた語」は Set ではなく **キー → 挿入した印の要素** の Map で持つ。GitHub がサイドバー等を描き直すと印ごと消えることがあり、記録だけが残ると二度と付かなくなるため。**その印がまだ説明として使えるか**を毎回確かめ、使えなければ付け直す（下の 3-2-3）
- GitHub は画面を読み直さずに差し替えることがある。**URL が変わったら画面全体を走査し直す**（URL の書き換えより先に内容が差し込まれることがあり、その分が飛ばされている可能性があるため）。ただし**「付けた語」の記録は空にしない**。GitHub はヘッダーやサイドバーを画面遷移をまたいで保持するので、そこに付いた印が残ったまま数え直すと本文に同じ語がもう一度付く。判定は「いま画面に、使える印があるか」だけで足りる
- コード表示部分を除外する。`<pre>` `<code>` に加え、現行の React 版コードビューア（`.react-code-lines` `.cm-editor` など）も対象
- 辞書に当たらないテキストでは可視判定（`getComputedStyle`）を呼ばない。全テキストノードでレイアウト計算を強制しないため
- 印は文字コードの記号ではなく CSS で描く。`U+1F6C8`（🛈）は Windows のフォントにはあるが macOS には無く、Mac では豆腐になっていた
- ツールチップは自前で描く。`title` 属性は表示までに 1 秒前後の待ちがあり、拡張側から短くできない
- 判定の部分（正規化・正規表現の組み立て・単複の引き直し・重複の除外）は [`src/matcher.js`](./src/matcher.js) に分けてある。DOM を触らないので、ブラウザと Node の両方から同じコードを呼べる。判定の規則を変えると `tests/matcher.test.js` が落ちる

## 3-2. 印の読み上げとキーボード操作

印は文章の中にも、リンクやボタンの中にも入る。同じ扱いにすると片方が壊れるため、入った場所で分けている。

| 入った場所 | 読み上げ | キーボード |
|---|---|---|
| ふつうの文章の中 | 印自体が入口。名前は「「merge」の解説」のように短くし、説明文は吹き出し側（`aria-describedby`）に置く（`role="button"`） | 印が Tab の順路に入る |
| リンク・ボタンの中 | 印を装飾として扱う（`aria-hidden`） | 印は順路に入れない。**その要素自体**に止まったときに説明を出す |
| 止まれない要素の中 | — | **注記しない。** 同じ語の次の出現に回す |

リンクの中の印に名前を与えると、GitHub 本来のリンク名（「Pull requests」）の後ろへ解説文が丸ごと足され、読み上げが長くなる。リンクの中へ `tabindex` を足すと、操作できる要素の入れ子になる。どちらも避けるための分岐。

**「操作要素らしいか」ではなく「実際にキーボードで到達できるか」で決める。** 入口を作れない場所では注記そのものを見送り、その語は「まだ説明していない」ままにして次の出現へ回す。

**判定はブラウザに任せる。** 要素名を並べたり `tabindex` 属性を自分で解釈したりすると、ブラウザの判断とずれる。基準は `el.tabIndex >= 0`（ブラウザが解釈したあとの値）で、その前に次を除く。

| 除くもの | 理由 |
|---|---|
| `input[type="hidden"]` | 画面に無い |
| `:disabled` に当たるもの | `fieldset[disabled]` の子孫は、要素の `disabled` が false のままでも無効。`:disabled` なら最初の `legend` の中だけ例外にしてくれる |
| `[inert]` の中 | 操作できない |
| `display:none` / `visibility:hidden` / 描画の箱が無いもの | フォーカスが当たらない。`opacity:0` は箱があるので、**入口の判定では**除かない（透明でもフォーカスは当たる）。ただし**語をどこに置くかの判定**では、祖先の `opacity:0` を不可視として除く（3-2-3） |

`tabindex=""` や空白だけの指定、`details` の外に置かれた `summary`、2番目以降の `summary` は、いずれもブラウザが `tabIndex = -1` と解釈する。自前で `Number(属性)` を計算すると、これらを取り違える。

**矢印キーで移動する部品（roving tabindex）は、入口として扱わない。** `role` と容器と `tabIndex 0/-1` の並びが揃っていても、到達できる証明にはならない。実際に辿り着けるかは「矢印キーを受けて `focus()` を動かす実装があるか」で決まり、それは DOM の形からは分からない。構造だけを見て認めていた版（v1.8.2）は、**構造は正しいのに矢印で動かない部品**を到達可能と誤判定していた（外部監査が反例で実証し、こちらでも実キーを送って再現した）。

そのため `tabIndex < 0` のものは入口にしない。**この判断で注記が減らないことは実サイトで測った**: GitHub の4ページ（トップ・大きな README・ファイル表示・ツリー）で、v1.8.2 が roving 判定に頼っていた注記は **0 件**だった。ファイル一覧の `readme` も、入口側（`tabIndex === 0` の項目）に付いている。v1.8.2 と v1.8.3 で、注記される語の集合は3ページとも**完全に一致**する。

**速さのための約束事**: 描画されているかの確認（`getComputedStyle` と `getClientRects`）はレイアウトを強制するので高い。**しかし省かない。** v1.8.2 では「印を入れる場所の先祖は `isTarget` が確認済み」として省いていたが、これは誤りだった——`isTarget` が見ているのは**テキストの直接の親**だけで、そこから上は別の要素である。`display:contents` の先祖は箱を作らず、`visibility:hidden` の先祖の中で子だけ可視に戻すこともできる。どちらも「子は見えているが、その先祖は Tab の順路に入らない」状態になる。

代わりに、**走査1回のあいだだけ有効な `WeakMap`** に測定結果を覚えておき、同じ要素を測り直さないようにした（`renderCache`）。走査は同期的に走るので、その間に測定結果が変わることはない。走査を抜けたら必ず捨てる（持ち越すと、GitHub が画面を差し替えたあとに古い値で判定してしまう）。当時の実測（開発機・macOS）では、正しさを戻したうえで大きなページが 16ms → 26ms、他の2ページは 9→11ms・9→10ms だった。**ただし「50ms 未満」は機械に依存する**——同じページを CI の ubuntu ランナーで測ると、v1.8.4 で 86ms、v1.8.5 で 77ms かかる（6-1-6）。

E2E は、この判定を**同じ式で計算し直さない**。実際に Tab キーを送り、ブラウザが止まった要素だけを「到達できる」とみなす（実装とテストが同じ思い違いを共有しないため）。

この判定で、実測から分かった落とし穴が2つある。

- **「フォーカスできる要素」をすべて入口にしてはいけない。** GitHub は本文を `tabindex="0"` の大きなスクロール領域（`#repos-split-pane-content`）で包んでいる。これを入口に含めると、**本文中の印11個がすべてその容器1つにぶら下がり**、文章の中の印から個別に読めなくなった（2026-08-05 実測）。入口の候補は**中身から読み上げ名が決まる操作要素**（リンク・ボタン・`role="tab"` など）に限る。容器は名前を中身から作らないので、その中の印はふつうの文章として扱う。
- **`tabindex="-1"` でも到達できる部品がある。** ファイルツリーやタブは、項目のうち1つだけが Tab で止まり、残りは矢印キーで移動する（roving tabindex）。これを到達不能と決めつけたところ、GitHub のファイル一覧にある `readme` が注記されなくなった（2026-08-05 実測）。
  - **その後の訂正（v1.8.3）**: この観察から「構造が揃っていれば到達可能とみなす」という規則を作ったが、それは行きすぎだった。構造は同じでも矢印に応答しない部品があり、そこで誤判定していた。2026-08-06 に測り直したところ、**`readme` が落ちた原因は roving 項目の除外そのものではなかった**——現在の GitHub では `readme` の入口は `tabIndex === 0` の項目に付いており、roving 判定に頼っている注記は4ページで0件だった。いまは `tabIndex < 0` を入口にせず、それでも注記される語は1つも減っていない。

**1つの入口に複数の用語があるとき**（`Merge pull request` に `merge` と `pull request` の両方が付く場合）、その入口にフォーカスすると**全部をまとめて出す**。印そのものに触れたときは、その1件だけを出す。以前は先頭の1件しか読めなかった。

説明は表示中だけ `aria-describedby` で結びつける。**属性は空白区切りの集合として足し引きする**——丸ごと保存して戻すと、表示中にページ側が足した ID を消してしまう。吹き出しの ID は読み込みごとに変えるので、ページ側の要素と衝突しない。

## 3-2-3. 「その語が実際に読めるか」で判定する（v1.8.4）

同じ語はページで最初の1回しか注記しない。だから**見えていない場所に印を付けると、その語はページのどこでも説明されなくなる。** 見えない側に付いた印は誰にも読めず、後ろにある読める同じ語は「もう説明済み」として飛ばされる。

v1.8.3 までは、語の出現場所の可視性を**直接の親だけ**で判定していた。これでは足りない。

| 隠し方 | 子の computed 値 | 子の箱 | 直接の親を見る判定 |
|---|---|---|---|
| 祖先が `opacity: 0` | `opacity: 1`（継承しない） | あり（1264×24） | **見抜けない** |
| 祖先が `content-visibility: hidden` | 変化なし | あり（1264×24） | **見抜けない** |
| `inert` の中 | 変化なし | あり | **見抜けない** |
| 1px 四方＋`clip`（読み上げ専用） | 変化なし | あり（1×1） | **見抜けない** |

いずれも 2026-08-06 に Chrome 151 で実測した。**箱の有無では判別できない**のが要点で、`getClientRects()` を見ても解決しない。

そこで次のようにした。

- **`Element.checkVisibility({ opacityProperty, visibilityProperty })`** を使う。祖先までまとめて見てくれるので、`opacity:0` と `content-visibility:hidden` はここで落ちる
- **`contentVisibilityAuto` は渡さない。** 渡すと `content-visibility: auto` の画面外要素まで不可視扱いになり、長いページの下のほうが永久に注記されなくなる（実測で確認したので、`verify.mjs` が渡していないことを検査する）
- **`inert` は `checkVisibility` では落ちない**（実測で `true`）。`SKIP` セレクタへ `[inert]` を足して、文字列を読む前に除外する
- **1px 四方＋`clip` も落ちない。** クラス名は版ごとに変わる自動生成なので、名前ではなく形で判定する（`isClipHidden`）

> **これは実際の GitHub で起きていた。** `prc-src-InternalVisuallyHidden-…` の中にある読み上げ専用の "Repository files navigation" に印が付いており、目に見える `repository` より先に「最初の1回」を使い切っていた（2026-08-06 実測・2ページで各1件）。直したあと、同じ3ページで**注記される語の集合は1語も変わらず**、見えない領域の中の印は 2 個 → 0 個になった。

**付けた印がまだ使えるかも、毎回確かめる。** `isConnected` は「DOM に残っている」ことしか示さない。祖先が `display:none` になっても、`opacity:0` になっても、`inert` の中へ移されても true のままである。それを説明済みの証拠にすると、**後から現れた読める同じ語に説明が付かない**（実測で再現）。いまは次をすべて満たすときだけ「使える」と見なす。

- DOM にあり、その場所が実際に見えている
- 単独の印なら、その印自身が Tab の順路に入る
- 装飾扱いの印なら、対応する入口が生きていて Tab の順路に入る

使えなくなった印は取り除いてから付け直す（残すと同じ語の印が画面に2つある状態になる）。印は `<sup>` 1つで中に文字を持たないので、取り除いても本文は壊れない。

## 3-2-3b. 箱を作らない要素の可視性を、性質ごとに分ける（v1.8.6）

`display:contents` の要素は箱を作らない。Chrome の `checkVisibility()` はこれに **false** を返すが、それは「隠れている」ではなく「箱が無い」という意味なので、そのまま答えに使えない。

v1.8.5 は、箱を持つ最も近い先祖へ聞いた**1つの答え**を子へ転用していた。これは両方向に誤る。

| 形 | 先祖に聞いた答え | 実際 | v1.8.5 |
|---|---|---|---|
| 先祖が `content-visibility: hidden`、中の `display:contents` に語がある | 見えている（先祖自身は描画されたまま） | 読めない | **見えない場所へ印を付ける** |
| 先祖が `visibility: hidden`、子が `visibility: visible` に戻している | 見えていない | 読める | **読める語を落とす** |

**Range の矩形でも見抜けない。** `content-visibility:hidden` の中身は、一度描かれてから隠された場合、`Range.getClientRects()` が**古い矩形を返し続ける**（実測: 隠す前 1 個 / 隠した後も 1 個）。最初から隠れている場合は 0 個になるので、矩形の有無は場面によって答えが変わる。

そこで、1つの答えを転用せず、性質ごとに聞き分ける。

| 性質 | 継承するか | どこに聞くか |
|---|---|---|
| `visibility` | する | **対象要素自身**の computed 値（子が `visible` に戻していれば、それが正しい） |
| `display:none` / `opacity` / 先祖の `content-visibility` | しない | 箱を持つ最も近い先祖に `checkVisibility({ opacityProperty })` で聞く。**`visibilityProperty` はわざと外す**（外さないと、子が戻した `visible` が先祖に潰される） |
| 先祖**自身**の `content-visibility: hidden` | — | その先祖は描画されたままなので `checkVisibility` では落ちない。名指しで見る |
| 文字が実際に描かれているか | — | 対象要素の Range |
| 切り取り | — | 対象要素から上へたどる（`isClipHidden`） |

`content-visibility: auto` は落とさない。画面外というだけで永久に除外すると、長いページの下が注記されなくなる。

## 3-2-3c. `clip` は絶対配置にしか効かない（v1.8.6）

CSS2 の `clip` は **absolutely positioned elements にだけ**適用される。`position` が `static` や `relative` のままの要素に `rect(0 0 0 0)` と書いても、中身はふつうに見えている。

v1.8.5 は配置を見ずに「全面の切り取り＝非表示」と判定していたため、**読める文章のほうを除外していた**（実測: `position:static`・幅 1264px の要素にある語へ印が付かなかった）。いまは `position` が `absolute` か `fixed` のときだけ `clip` を見る。`clip-path` は配置に関係なく効くので、そちらは位置を問わない。

判定が正しいことは、こちらの式の言い換えではなく**ブラウザの当たり判定**で確かめている。同じ `clip` 指定で `position` だけを変えた3つを並べ、`elementFromPoint` が `static`／`relative` では要素に当たり、`absolute` では当たらないことを見る。

## 3-2-3d. 動作に必要な最低の Chrome（v1.8.6）

`Element.checkVisibility` は Chrome 105 で入った。`manifest.json` に `minimum_chrome_version: "105"` を書いてある。

- これが無い環境で何が起きるかは実測した。同じ拡張の content script として `checkVisibility` を取り除いてから本体を読み込ませ、8つの場面を比べると、**祖先の `content-visibility:hidden` と `opacity:0` の2つで逆の答えになる**（見えない側へ印が付き、後ろの読める語が説明されなくなる）。`display:none` は代替手段でも見抜ける。
- つまり「直接の親だけを見る」程度の代替手段では支えられないので、最低版を書くほうを選んだ。
- 引数の綴りは 105〜120 と 121 以降で違う（`checkOpacity` / `checkVisibilityCSS` と `opacityProperty` / `visibilityProperty`）。**両方をまとめて渡している**ので、どちらの版でも効く。知らない項目は無視される。Chrome 151 で、旧綴りだけでも新綴りだけでも同じ答えになることを実測した。
- **これより古い Chrome を使っている既存の利用者には、この更新が配信されない。**

## 3-2-3e. 印を片づけるときは、注記したときの記録だけを使う（v1.8.6）

印を入れるとき、`splitText` で文字列を「用語で終わる左側」と「その後ろ」に割る。片づけるときは、割ったものを走査対象へ戻さないと、その語はページを開いているあいだ二度と説明されない。

v1.8.5 は、片づける時点の `previousSibling` / `nextSibling` を「自分が割った対」とみなしていた。DOM は誰でも触れるので、この推し量りは成り立たない。実測した被害は3つ。

| 起きたこと | 原因 |
|---|---|
| ページ側が印の隣へ置いた Text node を消していた | 隣にあるものを自分のものと見なして統合した |
| 利用者が選んでいた一文が空になった | その統合で、選択の端にあった節点を消した |
| ページ側が印だけを外すと、その語が二度と説明されない | 隣をたどれず、`handled` を解放できなかった |

いまは、注記した時点で**自分が何を作ったか**を控える。

```text
{ key, term, icon, parent, termNode, tailNode, splitOffset, placementKind, trigger }
```

そのうえで、次を守る。

- 片づけるときに**現在の隣接から推し量らない**。控えた `termNode` を `handled` から外すだけ
- **割ったものを繋ぎ直さない。** 繋ぎ直すには節点を1つ消す必要があり、そこに選択範囲やページ側の参照があると壊れる。文字数は割っても変わらないので、割ったままでよい
- 外すのは自分が入れた `<sup>` だけ。ページ側が作った節点には触れない
- 印が既に外されていても、記録があるので元の場所へ戻せる。`MutationObserver` で**取り除かれた節点も見て**、自分の印が外れていたら片づけて、その場で見直す
- **用語が末尾ちょうどで終わるときは割らない。** 割ると空の節点が1つ増え、片づけと付け直しを繰り返すたびに増え続ける

一致が複数ある節点では、**前から順に**処理する。後ろから割ると、先に控えた節点があとの分割でさらに割られ、記録が実物とずれる。

## 3-2-3f. 印が居なくなったら、既にある候補へ引き継ぐ（v1.8.7）

同じ語はページで最初の1回だけ注記する。そのために、**既に説明済みの語しか含まない節点も「処理済み」として記録**している。この記録を永久に残すと、最初の印を含む場所がページから消えたとき、2番目の候補が二度と選ばれない。実測では、`branch` を2か所に置いて最初を削除すると、ページ全体の印が **0個**になった（URL を変えて全体を走査し直しても戻らない）。

そこで、処理済みの記録を**世代つき**にした。

| | v1.8.6 まで | v1.8.7 |
|---|---|---|
| 処理済みの持ち方 | `WeakSet<Text>` | `WeakMap<Text, 世代>` |
| 飛ばす条件 | 集合に在る | **今の世代で**処理済み |
| 世代を進めるとき | — | 正規の印が退役したときだけ |

ふつうの変更では世代を進めないので、走査の費用は変わらない。退役は稀なので、そのときだけ `document.body` を選び直す。

**印と語の対応が壊れていないかも、毎回確かめる。** 印が DOM に在ることは、その印がまだ意味を持つことを示さない。実測した壊れ方は2つ。

| 壊し方 | v1.8.6 の結果 |
|---|---|
| 語の Text node だけを削除 | 語の無い場所に印が残り、後から現れた本物の語を「説明済み」として抑止した |
| 語と印のあいだへページが節点を挿す | 印が語ではなく、挿し込まれた文字の直後に位置した |

いまは記録全体（印・語の節点・親・語の位置・並び順・入口）を照合し、食い違えば退役させて選び直す。**ページ側の節点は動かさない**——自分の印を外し、選び直しで語の直後へ入れ直す。結果として印は語の直後へ戻り、ページが置いた節点はそのまま残る。

**複製された「自分のふり」を自分のものとして扱わない。** ページが注記済みの領域を `cloneNode` すると、印も入口の名札も複製される。実測では同じ入口 ID を持つ要素が2つになった。自分が作った印と入口を `WeakSet` で持ち、追加された領域を走査する前に、所有していないものを取り除く（触るのは自分のものだけ）。

## 3-2-3g. 切り取りは、書き方ではなく面積で決める（v1.8.7）

v1.8.6 までは `rect(0 0 0 0)` と `inset(50%)` という**決まった書き方**を文字列で照合していた。同じ「面積0」でも書き方はいくらでもある。

| 形 | v1.8.6 | 実際 |
|---|---|---|
| `clip: rect(5px,5px,5px,5px)` | 見えている扱い | 面積0（`right <= left`） |
| `clip-path: inset(100%)` | 見えている扱い | 面積0 |
| `display:contents` 自身の `clip-path: inset(50%)` | 隠れている扱い | **効かない**（箱が無い） |

いまは値として解き、`rect` は `right <= left` か `bottom <= top`、`inset` は縦か横の合計が100%以上を面積0とする。`auto` や、長さと百分率が混ざる形は**断定しない**（箱の大きさ抜きには決められないため）。`display:contents` の要素自身の切り取りは適用しない。

判定が正しいことは、こちらの式の言い換えではなく**ブラウザの当たり判定**で確かめている（文字が描かれている場所を `elementFromPoint` で突く）。なお `caretRangeFromPoint` は `clip-path` を無視して切り取られた文字にも当たるので、可視の判定には使えない（実測）。

## 3-2-4. 除外を決める前に、文字列を読まない（v1.8.4）

**「書き換えない」と「読み取らない」は別のこと。** v1.8.3 までの `isTarget` は、最初に `node.nodeValue` を取り出してから `SKIP` の判定をしていた。印は入らず内容も変わらないが、編集中のコメントや `textarea` の中身は、除外が決まる前に一度 JavaScript へ読み込まれていた。公開しているプライバシーの説明は「読み取りも書き換えもしない」と書いていたので、実装と食い違っていた。

いまは順序を逆にしてある。

```text
handled か  →  親要素があるか  →  SKIP に当たるか   ← ここまで文字列に触れない
        ↓
   nodeValue を読む  →  空白だけか  →  辞書に当たるか  →  実際に見えるか
```

**除外領域より後は、読んでから見えるかを判定する。** 全テキストノードで先に可視性を測ると、レイアウト計算が全ノードで走りページが重くなる。そのため、`display:none` や `opacity:0` の中の文章も**一時的には読む**。読んだ結果に印を付けるかどうかだけを、可視性で決めている。この範囲は `PRIVACY.md` にそのまま書いてある（「表示されている文章だけを読む」とは書かない）。

順序が戻っていないことの担保は2段構えにしている。

| 担保 | 何を見るか | 強さ |
|---|---|---|
| `tests/e2e` の「除外する領域のテキストが、辞書の照合まで届かない」 | **実際に辞書照合へ渡されたかどうか**。同じ拡張の content script として `matcher-tap.js` を本体より先に読み込ませ、隔離された世界の中で `matcher.test` / `findHits` に届いた文字列を記録する | **本命** |
| `scripts/verify.mjs` の順序検査 | `isTarget` の中の文字位置 | **補助**。別名の変数・補助関数・bracket 記法などで迂回できるので、これだけでは順序を保証できない |

ページ側から getter を差し替えても、content script は隔離された世界で動くため中を観測できない。だから計測用の JS は「同じ拡張の content script」として入れている。`tests/` にあり、配布物には入らない。

**測る場所を、出口から入口へ変えた（v1.8.6）。** v1.8.5 までは「文字を取り出せる口」を6つ塞いで数えていた。しかし取り出し口は `innerText` / `Range.toString()` / `substringData()` / `select` の `value` / `defaultValue` / `getAttribute('value')` …と際限がなく、6つ塞いだだけでは「全部塞いだ」と言えない。塞ぎ忘れた口を1つ通れば計測をすり抜ける。

読み取り方が何であれ、その文字列で語を判定するなら必ず `matcher` を通る。そこで `matcher` の入口を1か所押さえることにした。効いていることは2通りで確かめている。

- **わざと漏らす対照**（`leak-probe.js`）: 上の3つの取り出し方で除外領域の文字列を照合へ渡し、そのすべてが記録されることを見る。これは常設のテストとして残してある
- **本体を書き換える対照**（使い捨て）: `src/content.js` の `isTarget` に、除外判定より前で `innerText` / `Range.toString()` / `substringData()` を使う行を1つずつ入れ、そのたびに E2E が赤になることを確認した。確認後は元へ戻してある

## 3-2-3h. 「整っている」と「使える」を分ける（v1.8.8）

v1.8.7 は、記録が**形の上で**整っているかだけを見て「説明済み」を決めていた。形は整ったまま、意味だけが失われる場面がある。

| 何が起きたか | v1.8.7 の結果 |
|---|---|
| 印のある場所を `display:none` にする | 隠れた場所に印が残り、読める2番目の候補は **0個**のまま |
| 装飾扱いの印の入口を `disabled` にする | 押せない入口に印が残り、後ろの読める語は **0個**のまま |
| 語のうしろへ `appendData` で文字を足す | 印が `A rebasePAGE_SUFFIX` の直後に残り、後から現れた本物の語を抑止 |
| `label` の `for` を別の入力欄へ変える | 説明の入口が古い入力欄のまま。新しい入力欄からは説明へ到達できない |

そこで判定を2段に分けた。

- **整合（安い）** — DOM の形だけ。つながり・親・語が節点の**末尾**にあること・印が語の直後にあること。レイアウトを起こさないので、変更のたびに全記録（最大61件）へ掛けても軽い
- **使用可否（高い）** — いま見えるか・Tab で到達できるか・**その場所から解決される入口が、記録した入口と同じか**。`getComputedStyle` と `getClientRects` を使うので、必要なときだけ

「必要なとき」は、**属性が変わった・文字が書き換わった・ノードが外れた**変更が混ざっているとき、画面遷移のとき、ON へ戻したとき。ノードが増えただけの変更では見え方は変わらないので、安いほうだけを回す。自分が出し入れする吹き出しや、自分が付ける `role` / `tabindex` は数えない（数えると、カーソルを動かすたびに全記録のレイアウト計算が走る）。

**見張る対象も足した。** v1.8.7 は `childList` と `subtree` しか見ていなかったので、語そのものの書き換えには次の DOM 変更まで気づけず、属性による不可視化にはまったく気づけなかった。いまは `characterData` と、見え方に関わる属性（`style` `class` `hidden` `inert` `aria-hidden` `disabled` `tabindex` `for` `href` `role` `open` `contenteditable` `id`）を見る。**選び直しは1回の変更のかたまりにつき最大1回**に制限してある（50往復の混合変更で、同時に存在する印が常に0か1、変更を止めれば数が動かなくなることを E2E で確かめている）。

**注記したあとで、その場所が触れない領域へ変わることもある。** `isTarget` は走査の入口で除外を先に見るが、記録の整合を確かめる側が同じ順序を守っていなかったため、`contenteditable` へ変わった要素の本文を読んでいた（隔離世界で `nodeValue` の getter を包んで実測）。いまは整合の判定でも、**文字に触れる前に**除外領域かどうかを見る。`scripts/verify.mjs` が両方の順序を検査している。

## 3-2-3i. 自分のものかどうかを、class や属性の値で決めない（v1.8.8）

v1.8.7 は `.iiyaku-icon` という class と `data-iiyaku-trigger` という属性の値で、自分のものを見分けていた。どちらもページ側が自由に使える。実測した被害は4つ。

| ページ側がしたこと | v1.8.7 の結果 |
|---|---|
| `class="iiyaku-icon"` のリンクを後から足す | **消された** |
| 同じリンクを押す | クリックを横取りされ、リンク先へ移動しなかった |
| 2つのリンクに同じ `data-iiyaku-trigger="shared"` を置く | どちらの入口でも**両方の語の説明**が出た |
| 値に `"` や `]` を入れる | その値で組んだ selector が `SyntaxError` になりうる |

いまは、印と入口の結び付きを**内部の表**（`WeakMap` / `WeakSet`）だけで持つ。ページ DOM の属性は読まない。自分が作った印には読み込みごとに変わる合言葉（`data-iiyaku-owner`）を入れ、**合言葉があって、かつ自分が作ったものではない**ものだけを複製として取り除く。ページ側が同じ class を使っていても触らない。入口には目印（`data-iiyaku-entrance`）を**書くだけ**で、そこから入口を探すことはしない。既に同じ名前の属性があれば上書きせず、その入口を指す記録が無くなったら目印を外す。外部から来た値を CSS selector へ埋めることはしない。

## 3-2-3j. 切り取りの判定は、箱の大きさへ換算する（v1.8.8）

v1.8.7 は `inset()` の値がすべて百分率のときしか判定しなかった。ところがブラウザが返す値は `inset(50% 0 50% 0)` と書いただけで `inset(50% 0px)` になる。**長さと百分率が混ざった瞬間に判定を降りていた**ため、全面が隠れているのに可視と答えていた。

| 書き方 | computed 値 | v1.8.7 | v1.8.8 |
|---|---|---|---|
| `inset(50% 0 50% 0)` | `inset(50% 0px)` | 隠れた側に印（誤） | 後ろの読める語へ |
| 100×100 の箱へ `inset(50px)` | `inset(50px)` | 隠れた側に印（誤） | 後ろの読める語へ |
| `inset(0 100% 0 0)` | `inset(0px 100% 0px 0px)` | 隠れた側に印（誤） | 後ろの読める語へ |
| `circle(0)` | `circle(0px)` | 隠れた側に印（誤） | 後ろの読める語へ |
| `inset(10%)` | `inset(10%)` | そのまま注記（正） | そのまま注記（正） |
| `display:contents` 自身の `inset(50%)` | — | 効かせない（正） | 効かせない（正） |

いまは border box の寸法（`offsetWidth` / `offsetHeight`）へ換算し、`top + bottom >= 高さ` か `left + right >= 幅` なら面積0とする。寸法が取れないときは百分率だけで言えることに限る。`circle()` と `ellipse()` は半径が0なら面積0とする。

**切り取りは「指定が面積0か」ではなく、語の矩形との交わりで決める（v1.8.12）。** v1.8.11 までは、祖先ごとに「その切り取りが全部を消すか」だけを見ていました。しかし**切り取りに余地があっても、その語が枠の外に置かれている**ことがあります。実測（画面の画素を数えた）では、`overflow: hidden` / `overflow: clip` / 面積のある `clip-path` の外に置いた語は**1画素も描かれていない**のに印が付き、後ろの読める同じ語が説明されませんでした。

いまは祖先をさかのぼり、`overflow`（`hidden` と `clip` のみ）・legacy `clip`・`clip-path` が課す切り取りを **viewport 座標の矩形**として積み上げ、語そのものの矩形（`Range.getClientRects()` の面積があるものだけ）と交わるかで決めます。交わりが 1px 以下の帯しか残らないなら、読める文字は残りません。これで、これまで個別に見ていた「面積0の切り取り」（`rect(0 0 0 0)` / `inset(50%)` / `inset(100%)` / `circle(0px)` / 潰れた参照ボックス）は、特別扱いなしに落ちます。

落としすぎないための約束も一緒に置いています。**`overflow: auto` / `scroll` は切り取りに数えません**（スクロールすれば読めるので、画面外というだけで永久に除外すると長い一覧の下が説明されなくなる）。**絶対・固定配置は、包含ブロックでない祖先の切り取りから逃げます**。大きさの目安（1px 四方）だけで不可視と決めるのもやめました——`clip-path: inset(-100px)` で外へ広がって**362画素が実際に描かれている**語を落としていたためです。

**描画効果は、断定できるものだけを不可視とします（v1.8.12）。** `filter: opacity(0)` と、色がすべて透明な単純なグラデーション mask。`transform: scale(0)` は、文字の矩形に面積を求めることで落ちます（箱の寸法は変わらないので、面積を見ないと落とせません）。それ以外の `filter` / `mask` は見抜けないので、§5 に限界として書いてあります。

**参照ボックスが「潰れている」ことと「分からない」ことを分けた（v1.8.11・いまは矩形の式に吸収）**: `refBoxSize()` は content box の幅・高さに 0 を返しうる。v1.8.10 はこれを「寸法不明」と同じ扱いにしていたため、既知の 0×0 参照ボックスへ `inset(0)` を掛けた形——中身はまったく見えない——を可視と答えていた。寸法が分かっているなら 0 でもそのまま面積の式に入れる。負の inset で外へ広がる形も同じ式で正しく出る。

**半径を省いた形と、参照ボックスだけの形にも対応した（v1.8.10）**: `circle()` の半径を省くと既定は `closest-side` で、computed 値からは半径が消える（`circle(at 0px 50%)`）。中心から参照ボックスのいちばん近い辺までの距離が半径になるので、中心が辺の上にあれば面積は 0 になる。また `clip-path` は**参照ボックスだけを書くこともできる**（`clip-path: content-box`）。そのときはその箱の辺が切り取り線になるので、箱の幅か高さが 0 なら全面が消える。どちらも v1.8.9 は取りこぼしていた（実測で、隠れた側に印が付き、後ろの読める語は 0 個だった）。

**参照ボックスとキーワード半径にも対応した（v1.8.9）**: `clip-path` は `<basic-shape> || <geometry-box>` を取り、computed 値は `inset(50%) content-box` のように参照ボックスが後ろに付く。百分率はその箱の実寸に対して解決する（`content-box` の 50% は padding を除いた内側の 50%）。`ellipse(0 closest-side)` のようにキーワードと数値が混ざる形も、片方が 0 なら面積 0 と判定する。

**既知の制約（断定しない側へ倒す）**: `calc()` を含む値、`polygon()`、`path()`、`shape()` は解かない。値だけからは面積を決められないので、可視として扱う。その結果、`clip-path: inset(calc(50% + 1px))` のような書き方で隠された語は、いまも「ページで最初の1回」を消費する。実測で確認済みの残る穴である。

## 3-2-3k. 見え方が変わる合図を、1か所へ集める（v1.8.9）

v1.8.8 は「決まった属性が変わったとき」「文字が変わったとき」「ノードが増減したとき」だけを合図にしていた。実測すると、これでは足りない場面が7通りあった。

| 何が起きたか | v1.8.8 |
|---|---|
| `input.type` を `hidden` にする | 見張る属性の一覧に `type` が無く、印が残った |
| 任意の `data-state` で `display:none` にする | 一覧に無いので、印が残った |
| `checked` プロパティ＋`:has()` で祖先を隠す | 属性にも DOM にも出ないので、気づけない |
| 子を1つ足すだけで祖先が消える（`:has()`） | 「増えただけなら見え方は変わらない」と決めていたので、印が残った |
| CSS の遷移で `opacity` が 0 になる | DOM は何も変わらないので、印が残った |
| 画面幅が変わって media query が入れ替わる | `resize` は吹き出しの位置合わせしかしていなかった |
| `<head>` へ stylesheet が足される | `body` しか見張っていないので、気づけない |

**属性の絞り込みをやめた。** どの属性が見え方に効くかは CSS 次第で、一覧では追いつけない。あわせて、CSS の遷移・アニメーションの終わり、画面の大きさの変化、`<head>` の stylesheet、利用者の操作（`input` / `change` / `click`）も合図にし、**すべて1つの予約口へ集める**。1回のまとめ直しでやることは、複製の除去・整合の確認・見え方の確認・世代を進める・全体の選び直しが**それぞれ最大1回**である。

**属性にも DOM にも出ない変化**（`checked` のようなプロパティだけの書き換えを、利用者の操作なしに行う場合）は、どの合図にも乗らない。ここは**暇なときの確認**でしか拾えないので、`requestIdleCallback` で 2 秒に1度だけ、記録の見え方を確かめ直す（画面が見えていないときと、記録が無いときは何もしない）。**即座ではない**という制約は残る。

## 3-2-3l. 「自分の要素」と「自分が起こした変更」は別（v1.8.9）

v1.8.8 は、変更の対象が自分の印なら自分の仕業とみなしていた。`MutationRecord` に変更の主体は載らないので、これは推し量りである。実測では、**ページ側が正規の印へ `style="display:none"` を書くと、見えない印が「説明済み」として残り、後ろの読める語が抑止された**。

いまは、自分が書くつもりの「要素 + 属性名 + 値」を控えておき、**その3つが完全に一致する変更だけ**を自分の仕業として無視する。予定と違う値になっていたら、書いたのは自分ではないので、見え方を確かめ直す。

## 3-2-3m. 複製の後始末と、印の見た目の条件（v1.8.9）

OFF のあいだは見張っていないので、その間に注記済みの領域が複製されると、ON に戻したときに正規の印と複製の印が並ぶ（実測で 2 個）。ON に戻すときは、走査より前に**ページ全体から複製を取り除く**。

> **v1.8.10 で、複製の見分け方を変えました。** v1.8.9 は合言葉（`data-iiyaku-owner`）の**値**で複製を見分けていたので、値を書き換えた複製も、値を消した複製もすり抜けました。後者は幅0になるものの `role="button"` と `tabindex="0"` を保つため、**見えないのに Tab で止まる**点になります（実測）。**v1.8.12 でさらに変えました。** 説明文は公開物なので所有の証明になりません（→ 3-2-3n）。

## 3-2-3n. 複製は「自分の側の証拠」だけで見分け、ページの持ち物は消さない（v1.8.12）

> v1.8.10〜v1.8.11 は、**辞書のキーとその説明文が一致していること**を複製の証拠にしていました。「ページが偶然その文面を持つことはない」と書いていましたが、**偶然でなくても持てます**。実測では、ページ側の `<span data-iiyaku-key="fetch" data-iiyaku="<辞書の説明文>">PAGE DATA</span>` が起動時に**本文ごと消えました**。辞書の説明文は公開物なので、所有の証明にはなりません。

いまの判定は、**自分の側の証拠だけ**を使い、**消すのは自分の作ったものの複製だと断定でき、かつ中身が空のときだけ**にします。

1. **今回の読み込みの合言葉**（`data-iiyaku-owner` が現在の UID）を持ち、いま自分の正規の印ではない → 自分が作ったものの複製と断定できる。中身が空なら外し、**中身があるなら名札を外すだけ**にする（ページがその節点を作り替えて本文を入れていることがある）
2. 合言葉を消された／書き換えられた複製 → 断定できない。`<sup>`・自分の class・**中身が空**・押せる（`tabindex` か `role="button"`）の4つが揃ったものだけを扱い、自分の名札が1つでも残っていれば名札ごと、1つも無ければ**操作性だけ**を外す
3. ページ側が中身を入れている節点は、どちらの場合も消さない

**見た目も、3つとも今回の合言葉へ絞ります。** `styles.css` は合言葉の**有無**しか見られないので、走り出しに「値が自分のものでない要素からは自分の装飾を引き上げる」規則を1つ足します。v1.8.11 まではこの絞り込みが印にしか掛かっておらず、ページ側の `class="iiyaku-tooltip" data-iiyaku-owner="page"` の要素が `position: fixed` / `z-index: 2147483647` になっていました（実測）。`styles.css` が与える性質と、絞り込みで戻す性質の一覧が食い違わないよう、`verify.mjs` が両者を突き合わせます。規則がページから外された場合は、まとめ直しのたびに足し直します。

## 3-2-3n-2. 「作ったことがある」を、変更の出どころの証明に使わない（v1.8.12）

v1.8.11 までは、`madeIcons`（自分が作った要素の集合。退役しても取り消さない）を「その変更は自分が起こした」の証明に使っていました。**作ったのが自分でも、外したのは相手かもしれません。** 実測で3つの害が出ています。

- ページが正規の印だけを外しても「自分の後始末」と数え、**約2秒間その語の説明が消えた**まま戻らない
- 退役した印をページが本文として使い回すと、その領域を永久に走査対象から外し、**中の語が一度も説明されない**
- ページがその節点を DOM へ戻したとき、複製の後始末が素通りして**同じ語の印が2つ並ぶ**

いまは「作ったことがある」記録を持ちません。変更の出どころは次の2つだけで言います。

- **いま自分のものか**（`ownedIcons`。退役で取り消す）
- **自分が外す直前に控えたか**（`expectedRemovals`。1回だけ受け取って消す）

**記録の中身と意味も、毎回確かめます。** `isCoherent` は、印が `<sup>` で自分の class を持ち、辞書のキー・説明文・用語が記録どおりで、置き方に応じた `role` / `tabindex` / `aria-*` になっていることまで見ます。食い違ったら退役させ、正しい印を作り直します（本文には触れません）。確かめないと、ページが説明文を書き換えても正規の記録として残り、**誤った説明を出し続けます**（実測: 3秒後も `WRONG EXPLANATION` のままでした）。

## 3-2-3o. 見えるようになった語を探す（v1.8.10）

v1.8.9 は、見え方が変わる合図を受けたとき**既にある印を確かめ直すだけ**でした。走査し直す場所も、退役した記録も無い場合は何も走らないので、**最初の走査で隠れていた語は、そのタブを開いているあいだ永久に説明されません**（画面幅・stylesheet の除去・遷移・property だけの変化の4通りで実測）。

そこで、辞書には当たったがそのとき見えていなかった節点を控えておき、見え方が変わったときに**そこだけ**を見直します。ページ全体を走り直さないので費用が上限を持ちます。

見直す前に、文字列の照合だけで足切りします——印は1語につき1つなので、既に読める印がある語しか入っていない節点は、見えるようになっても何も足せません。この足切りにより、隠れた段落が500本ある頁でも1回あたり **+0.2〜0.3 ms** に収まります（実測・3組とも同じ向き）。

### 3-2-3o-1. 控えを見直すときも、読む前に確かめる（v1.8.11）

**控えの見直しは、本文へ触れる前に「いまも触れてよい場所か」を確かめます。** 走査の入口（`isTarget`）と同じ順序です。v1.8.10 は見直しの経路だけ順序が逆で、控えへ入れたあとにページがその場所を編集領域・`aria-hidden`・`inert`・`hidden`・コード領域へ変えると、**2秒ごとにその本文を読んでいました**（実測）。触れない場所へ移った候補は、読まずに控えから外します。戻ってきたときは、その属性変更が走査し直す場所として渡ってくるので、そこで入り直します。

### 3-2-3o-2. 控えるのは「見えなかった語」だけではない（v1.8.11）

**いま入口が無いだけの語**も控えます。押せないボタンの中の語、`tabindex="-1"` の部品の中の語、対応先の無い `label` の中の語は、見えてはいても印を入れる場所が決まりません。v1.8.10 はこれを処理済みにしていたので、**あとで押せるようになっても同じ世代では二度と見ませんでした**（実測: `disabled` を外しても説明が付かない）。いまは控えへ入れ、見え方や入口が変わったときに見直します。

なお `href` の無い `<a>` は入口の候補にならないので、その中の語は最初から**ふつうの印**（印自体を入口にする形）で説明されます。ここは「入口が無い」に当たりません。

### 3-2-3o-3. 上限では捨てない（v1.8.11）

控えの上限は 20,000 件です。**上限に達しても控えを捨てず、増やすのをやめるだけ**にします。v1.8.10 は上限で控えを丸ごと捨てていたため、そのあとは「候補0件」に見え、**property だけの表示変化を二度と拾えませんでした**。さらに、**既に控えてある節点を見直しのたびに数え直していた**ので、ちょうど上限の件数で必ずこれが起きました（実測）。いまは、既に控えてあるものは数えません。

**上限に達したことは旗に残し、空きができたら索引を作り直します（v1.8.12）。** v1.8.11 はこの旗をどこからも読んでいませんでした——立てるだけでは「もう探さない」が黙って続きます。いまは、控えをひと回りし終えた時点で旗が立っていて、かつ空きがあるなら全体を走り直して控えへ入れ直します。満杯のときは、まず死んだ控え（ページから外れた・処理済み）を落として空きを作ります。

⚠️ 旗を下ろすのは、**実際に入れ直したときだけ**です。空きが無くて引き返した1回で旗を下ろすと、あとで空きができても二度と入れ直しません（実装中に実測で再現しました）。

控えが一度も減らない頁では、上限を超えた分はなお説明されません。これは §5 に既知の限界として書いてあります。

### 3-2-3o-4. 控えの見直しは、時間の予算で区切る（v1.8.12）

控えが 20,000 件あるページでは、2秒ごとの見直しに毎回 32.7〜60.6 ms かかっていました（実測）。いまは **8ms の予算**で区切り、続きは次へ持ち越します。並びを控えて位置を覚え、続きは**マイクロタスクではなく一度ブラウザへ返してから**走らせます（マイクロタスクで続けると同じ処理の中で回り続け、区切った意味がありません）。同じ条件で 8.3 ms になりました。

### 3-2-3o-5. カーソルとフォーカスも合図にする（v1.8.12）

`:hover` や `:focus-within` だけで開くメニューは、DOM も属性も遷移も動かないので、どの合図にも乗りません。2秒ごとの確認より先に閉じるメニューは、一度も説明されませんでした（実測: 400ms 開いても印0）。

`pointerover` / `pointerout` / `focusin` / `focusout` を合図に足しました。ただし**控えが空なら何もせず**、カーソルは大量に動くので**1フレームに1回へまとめ**ます。

### 3-2-3o-6. `<html>` の属性も見張る（v1.8.12）

見張っていたのは `document.body`（とその子孫）と `document.head` だけで、**`<html>` の属性変更は1件も届いていませんでした**（実測: `data-color-mode` を変えて表示が消えても、まとめ直しは1回も走らず、暇なときの確認まで約2秒かかった）。見た目を切り替える指定は `<html>` に置かれることが多いので、ここも見張ります。

ただし**走査し直す場所には入れません**。入れると1回の書き換えでページ全体を歩き直すことになります。`<head>` の stylesheet と同じく「見え方だけが変わった合図」として受け、見えるようになった語は控えの見直しで拾います。

### 3-2-3o-7. 自分が書いた予定は、1回だけ受け取って消す（v1.8.12）

自分が書く属性は「要素＋属性名＋変更前の値＋変更後の値」の待ち行列に控え、`attributeOldValue` で受け取った変更前の値と突き合わせて**1回だけ**消費します。消さずに覚えっぱなしにすると、そのあとページが同じ値へ書き戻した変更まで自分の仕業として捨てます。見張っていないあいだに書いた分は、消費される機会が無いまま残るので、切り替えのときに捨てます。

## 3-2-3p. 自分が起こした変更を、もれなく数えない（v1.8.10）

印を1つ入れると、DOM には3つの変更が出ます——印が増える（`childList`）、本文が割れて右側の節点が増える（`childList`）、割られた左側が短くなる（`characterData`）。v1.8.9 は後ろ2つをページの変更として数えていたため、**印を1つ動かすたびに空のまとめ直しがもう1回走って**いました（実測: 1回隠すと2回。語が節点の末尾で終わる形＝割らない形では1回だったので、割ったことが原因だと切り分けられました）。

いまは、割ってできる2つの変更を「次に1回だけ起きるはず」として控え、受け取った時点で消します。属性の予定表と同じ考え方です。覚えっぱなしにしないのは、そのあとページがその節点を書き換えたときに気づけなくなるからです。

**そのためには、走査より先に見張り始めていなければなりません（v1.8.11）。** v1.8.10 は初回の走査を見張る前に行っていたので、そこで登録した「予定」は消費されないまま残り、**そのあとページが起こした最初の文字変更を自分のものとして捨てて**いました（実測: 語を消しても、次に暇なときの確認が来るまで印が動かない）。見張り始めてから走査する順序へ直しています。

## 3-2-3q. 吹き出しと切替ボタンは、要素そのもので見分ける（v1.8.10）

v1.8.9 は `.iiyaku-tooltip` / `.iiyaku-toggle` という**class 名**で自分の部品を見分けていました。名前は誰でも名乗れるので、ページ側が自分の段落に同じ class を付けただけで、その要素を自分の持ち物として扱っていました。実測では2つの影響が出ています。

- ページ側の本文が同じ class を名乗っていると、そこを**永久に走査しません**（除外一覧に class 名を並べていたため）。その語は後ろの段落へ回ります。
- ページがその要素へ加えた変更を「自分の変更」として無視するので、隠されたことに気づくのが**暇なときの確認まで遅れます**（実測 1,876 ms → 141 ms）。

いまは `tip` と `toggleBtn` の**要素同一性**で見分けます。CSS も合言葉つきの選択子へ寄せ、ページ側の同名 class には当たりません。

**v1.8.11 で、残っていた2か所も同じ形にしました。** 除外一覧に残っていた `.iiyaku-icon` を外し（ページ側の本文が同じ class を名乗るだけで、その中を一度も走査しなかった）、カーソルが吹き出しの中にあるかの判定も、class ではなく**いま出している吹き出しそのもの**で見ます（ページ側の同名要素へ移っただけで説明が閉じなかった）。自分の印は要素同一性で除外します。

## 3-2-2. 編集できる領域には触れない

**表示を助ける拡張は、利用者が書いている DOM を変えてはいけない。** 印を差し込むとテキストノードを分割するので、選択範囲・取り消し履歴・貼り付け・送信内容・フレームワークの内部状態に影響しうる。

除外するのは `textarea` `input` `select` と、`[contenteditable]:not([contenteditable="false"])` の要素とその子孫。`contenteditable` は `"true"` だけでなく、**属性だけ・`plaintext-only`・祖先からの継承**もあるため、`closest()` で上へたどって判定する。E2E でこの4通りと、起動の前後で中身が1文字も変わらないことを確かめている。

## 3-3. ON / OFF でページを読み直さない

v1.7.1 までは、切り替えのたびに `location.reload()` を呼んでいた。書きかけの Issue コメントが消え、スクロール位置も失われるため、状態の切り替えだけを行うようにした。

- OFF: 監視（MutationObserver）を止め、吹き出しを閉じ、`<html>` に付けたクラスで印を **CSS で隠す**
- ON: 隠すのをやめ、`document.body` を一度走査し、監視を再開する
- 印を DOM から**消さない**のは、消すと分割済みのテキストノードが元に戻らず、ON へ戻しても付き直さない語が出るため
- 保存に失敗したときは、表示を切り替える前の状態へ戻す（保存できていないのに見た目だけ変わる状態を残さない）
- `chrome.storage.onChanged` を購読しているので、片方のタブで切り替えると他の GitHub タブにも反映される

## 4. 辞書を増やす・直す

[`locales/dict.json`](./locales/dict.json) に `"英語キー": "日本語の説明"` を追記し、`chrome://extensions/` で更新を押す。

- キーは**小文字**で書く
- **複数形は自動では作られない。** 必要なら `src/matcher.js` の `EXTRA_FORMS` へ、そのキーの綴りを明示して足す。足す前に「GitHub の画面か Git の文書に実際に出るか」「無関係な意味の普通の英単語と衝突しないか」の2つを確かめ、理由をテストのコメントに残す。表に無い綴りは拾わない
- 空白を含むキー（`pull request` など）は、空白の数や種類が違っても一致する
- 先頭が記号のキー（`.gitignore` など）は単語境界の判定に引っかからないため、そのままでは機能しない

## 5. 既知の限界

- **継続的な使用による検証を経ていない。** 実機で確認したのは基本的な表示と動作まで
- 大きなページでの速度は 6-1-5 で実測した（初期走査 36ms・条件は同節）。ただし測ったのは headless の Chrome で3ページ分だけで、常用したときの体感は別
- 他の拡張との重なり: 英和辞典系の拡張が同じホバーで別のポップアップを出す場合、表示が重なる。z-index は最大値にしてこちらが前に出るようにしたが、相手も同値なら DOM 順で決まる
- Windows / Linux の実機での表示が未確認（字形の指定は下記のとおり明示済み）

### 3-4. 環境ごとの字形

説明文は日本語なので、**字形を持つフォントを OS ごとに明示する**。指定しないと GitHub 側の指定（Windows では `Segoe UI`）が効き、日本語の字形が無いために OS 既定へ落ちる。どれに落ちるかは環境任せで、行の高さや字面が揃わない。

| OS | 先に見るもの |
|---|---|
| Windows | `Yu Gothic UI` → `Meiryo` |
| macOS | `Hiragino Sans` → `Hiragino Kaku Gothic ProN` |
| Linux | `Noto Sans CJK JP` |

印の中の `i` は `Georgia` → `Times New Roman` → `Liberation Serif` の順で、**どの OS にも必ず1つはある**ので豆腐にならない（v1.7.0 で `U+1F6C8` が macOS に無く豆腐化した件の再発防止）。E2E で「指定した日本語フォントが計算後のスタイルに出ていること」「印が正方形のままであること」を固定している。
- 説明文が実際の初学者にとって分かりやすいかを検証していない
- 印は CSS で描いているため、`::after` の生成内容が本文のコピーに混ざらないはずだが、これは仕様からの推測で実測していない
- ツールチップは画面（viewport）の座標で置き、上下左右を余白の内側へ収めている。`body` に `transform` が掛かっている環境では `position: fixed` の基準がずれる可能性がある（GitHub 上では未確認）
- `styles.css` の `.iiyaku-tooltip` は v1.5 の改修で使われなくなり、CSS だけが残っていた。v1.7.0 で自前ツールチップを戻したことで再び使われている
- **支援技術（スクリーンリーダー）での読み上げを実測していない。** 印の役割分けは実装しているが、実際の読み上げソフトで確かめたのは未了

**いまの仕組み（上の「限界」と取り違えないための現況）**: DOM の変更は `queueMicrotask` で1つの予約口へまとめ、1回のチェックポイントにつき1回だけ走らせる。監視は `childList` / `subtree` / `characterData` / `attributes`（絞り込みなし・`attributeOldValue` つき）で、`<head>` と `<html>` の属性も別に見張る。初回に見えていなかった語・入口がまだ無かった語は控え（最大 20,000 件）へ入れ、遷移の終わり・画面幅・stylesheet・操作・カーソル・フォーカス・2秒ごとの確認で見直す。見直しは 8ms の予算で区切り、続きは次に持ち越す。
- **カーソルが要素の境目をまたぐたびに、控え全体を見直す。** 1回あたりの費用は控えの件数に比例する。`:has()` や兄弟結合子を使えばカーソルの位置と無関係な場所も変わりうるため、見直す範囲は狭められない。代わりに**使ってよい時間に上限**（直近2秒のうち200ms）を置き、さらに**何も見つからない見直しが続いたら間隔を空ける**（150ms → 最大 300ms・v1.8.17）。上限を 300ms に留めているのは、短時間ひらくメニューを取りこぼさないため。**またいだ回数への比例は残る**（実測: 5,000候補・40回で 115,122 回。ただしそのあいだの余分な実時間は 224ms で、回数は費用の代わりにならない）
- **到達できるかは、枠ごとに「箱と動かせる量」を持ち、内側から外側へ区間を伝播させて決める**（v1.8.17）。1つの矩形へ潰すと、入れ子の外側を見落とす。文書そのものも同じ形（箱＝画面）で最後に足す
- **スクロールの可動域は、軸ごとに実際の向きで見る。** 原点は片側にあり、その手前へは動かせない。**書字方向 5 × `direction` 2 の10通りを実測して規則にした**（v1.8.17）。横は「横書きなら `direction`、縦書きなら `rl` かどうか」、縦は「横書きなら常に正。`sideways-lr` だけ `ltr` で負、他の縦書きは `rtl` で負」
- **変形前の箱は `offsetWidth` / `offsetHeight` で取る**（v1.8.16）。整数へ丸められるので1px 未満のずれが出る。インライン要素・置換要素では最初の箱の寸法になる
- **変形の線形部分には、個別プロパティ（`rotate` / `scale`）も入れる**（v1.8.17）。`offset-path` に沿う回転と 3D は解かず、形の判定そのものをしない
- **形との交差は多角形で当てる**（v1.8.17）。矩形で当てると、回った場所では必ず広がる
- **一致した語の矩形は、空白で切って塗りのある並びごとに取る**（v1.8.17）。`pull request` のような2語のキーで、語間の空白まで語の位置に数えないため
- **切り取りは矩形として解けるものだけ解く**（v1.8.19）。`inset()` `circle()` `ellipse()` と参照ボックスは解く。`polygon()` `path()` `shape()` `calc()` を含む形と 3D の変形は**断定しない**
- **影と背景の抜きは「その語の位置に届くか」で見る**（v1.8.17／v1.8.20 で層ごとへ）。影はずらし量とぼかし、背景は**層ごとに** image / size / position / repeat / origin / clip を対応付けて塗る場所を出す
- **`mask` の合成（Porter-Duff）は解かない**（v1.8.17）。足し合わせ以外は「断定できない」とする
- **用語は、全部の並びが読めるときだけ可視とする**（v1.8.18）。`pull request` のような2語のキーで、片方だけ読める状態を可視として扱わない（第19回 RG-19-01）
- **印そのものが描かれているかは、入れてから実物を測る**（v1.8.18）。場所を予測して判定に入れたら、35°回した形の中で外れた。描かれていなければ引き取り、控えへ戻す
- **`scroll-snap-type: mandatory` の軸では、いまの位置でしか断定しない**（v1.8.18）。止まり位置そのものは解かず、スクロールが落ち着いてから見直す（第19回 RG-19-02）
- **変形前の矩形へ戻せない角度（ちょうど45°など）では断定しない**（v1.8.18）。以前は広い平行四辺形で代用しており、形の外の語を拾っていた
- **`background-size: auto` の画像は、自然寸法が同期で分からないので断定しない**（v1.8.18）。gradient の auto は領域いっぱいなので解ける
- **解けない `filter` の `url()` は可視へ昇格させない**（v1.8.18）。`mask-mode` は層ごとに見て、`luminance` の黒は 0 として扱う
- **控えの見直しには見え方の世代を持ち、状態が変わらなければ測り直さない**（v1.8.18）。カーソルの合図は、ページに `:hover` 等を使う規則があるときだけ動く。**ただし暇なときの確認では必ず世代を進める**（属性にも DOM にも出ない変化は、そこでしか拾えない）
- **断定できない候補には、印を付けない**（v1.8.19）。第20回 RG-20-01。以前は2周目で引き受け直していたが、そこを通ると 0画素の語が辞書のキーを取り、`tabindex="0"` の**見えない押せる点**になっていた。実際には見えているのに断定できない語は、ページに他の出現が無ければ説明されない——「見えない押せる点を作らない」ほうを取る
- **解けない切り抜きは「制限なし」ではない**（v1.8.19）。`polygon()` / `path()` / `shape()` / `calc()` を含む形、3D の変形は**断定しない**。ただし角を丸めていない `inset()` と参照ボックスだけの `clip-path` は**矩形そのもの**なので、外接矩形の交差だけで決まる（この2つを一緒くたにして、読める語を12件落とした）
- **印そのものの見え方も測る**（v1.8.19）。矩形・切り取り・到達性・形に加えて、印自身の `visibility` / `opacity` / `checkVisibility` と、**当たり判定による覆い**を見る。⚠️ 覆いと呼べるのは、印とも印の祖先とも関係のない要素が前に出ているときだけ。⚠️ 当たり判定は「**いまスクロールを動かさずに見えている印**」にだけ当てる
- **`mask` は一様でなければ断定しない**（v1.8.19）。gradient の値は場所で変わるので、要素全体の状態から語の位置は決められない。断片の URL（SVG の `<mask>`）も断定しない
- **背景は層ごとに対応付ける**（v1.8.19）。`image / size / position / repeat / origin / clip` を同じ層番号で対応付け、明示の寸法は実寸へ解く
- **stylesheet の形の指紋を見る**（v1.8.19）。`insertRule` / `deleteRule` は DOM に何も出さないので、枚数と規則の数で気づく。自分の style も、**生きた規則の数**が合わなければ入れ直す
- **擬似クラスの状態を覚える**（v1.8.19）。`:hover` が当たるのはポインタの祖先の連なりだけなので、同じ状態でひと回り終えていれば回さない。⚠️ 実測では**総回数は変わらなかった**（2秒ごとの確認と間引きが支配的）
- **見分けられない複製には触れない**（v1.8.17／v1.8.18・v1.8.19 でも持ち越し。⚠️ 第20回監査は「ストア提出前には閉じる必要がある」と裁定）。名札も合言葉も全部消された複製は、幅0で押せるだけの要素として残る。**その形の停止点はページが自分だけでも作れる**。構造として断つには印を closed shadow root の中へ入れる作り直しが要る（`cloneNode` は shadow root を複製しないので、複製された宿主は空の要素になり押せる点にならない）。印の同一性・意味づけ・見た目・イベントの経路がその上に載っているため、**単独の回で行う**
- **見た目は走り出しに読み込む**（v1.8.18）。`styles.css` は雛形で、**カスケードレイヤーの名前も選択子の合言葉も、読み込みごとに書き換えてから**入れる。固定名だったころは、ページが同じ名前のレイヤーを先に宣言することで順序を握り、**ページ自身の指定を消せた**（第19回 RG-19-06。実測: display:grid → inline・赤 → 黒・140×30px → auto）。選択子が合言葉の値まで見るので、ページ側の同名要素はそもそも一致せず、**打ち消す規則（`revert`）は要らなくなった**——打ち消す側は、ページ自身の指定まで巻き込む事故を2度起こしている
- **ON / OFF の目印は、属性の名前ごと読み込みごとに変える**（v1.8.16）。固定名だと、ページが同じ名前を使っているときに上書き・削除してしまう（実測で再現）
- **配布する CSS は固定の class 名で書かれているため、ページが読み込みの時点で `iiyaku-icon` などの class と `data-iiyaku-owner` 属性の両方を持つ要素を置いていると、こちらの JS が走り出すまでのごく短いあいだ、その要素へ自分の見た目が乗る**（実測: 走り出したあとは絞り込みの規則が外す）
- **控えの上限（20,000件）を超えた候補は、空きができるまで見つからない。** 上限に達したことは旗に残し、控えが減ってから索引を作り直す。空きが一度もできない頁では、超えた分は説明されない（実測: 20,001件目は、逃がし弁が1件注記されるまで見つからなかった）
- **属性にも DOM にも出ない変化は、その場では拾えない**（`checked` など）。2秒ごとの確認でのみ気づく
- 実サイトでの長時間の計測をしていない。合成した 20,000 件の控えでは、1回の見直しを 8ms の予算で区切って 8.3ms に収まることを実測した

**これまでに置き換えた仕様（履歴）**: 下は**もう当てはまりません**。現行仕様は上の一覧です。⚠️ 第21回 RG-21-08 で、この6件が現行の説明と同じ節に並んでいることを指摘されました——版を上げるたびに新しい行を足し、古い行を消していなかったためです。

- v1.8.19 まで: 解けない切り取りを「可視へ倒す」としていた（いまは断定せず、印を付けない）
- v1.8.19 まで: 3D の変形を「可視へ倒す」としていた（いまは断定しない）
- v1.8.16 まで: スクロール量を両方向へ対称に見積もっていた（いまは軸ごとに実際の向きで見る）
- v1.8.17 まで: ちょうど45°などの角度を、広い平行四辺形で代用していた（いまは断定しない）
- v1.8.18 まで: 背景の寸法を `auto` の場合だけ解き、それ以外は届く側へ倒していた（いまは層ごとに実寸へ解く）
- v1.8.18 まで: 断定できない候補の語を、まとめ直しの最後に引き受け直していた（2周目。v1.8.19 で廃止）


## 6. 検証

### 6-1. いま再現できる検証

clone した直後に `npm test`（依存パッケージなし）で実行できる。中身は次の3つで、GitHub Actions でも同じものを走らせている。

| 対象 | 実体 | 内容 |
|---|---|---|
| 構文 | `node --check` | `src/matcher.js` と `src/content.js` |
| 用語の判定 | [`tests/matcher.test.js`](https://github.com/Driedsandwich/repogloss/blob/main/tests/matcher.test.js) | 全 61 キーの自己一致、大文字小文字と空白、単複（`-s` `-es` `-y→-ies`）、長いキーが短いキーに食われないこと、同じ語の重複除外、`fork/forks` のように単複が別キーの語、一致位置の正しさ、部分一致しないこと |
| 構成と文書 | [`scripts/verify.mjs`](https://github.com/Driedsandwich/repogloss/blob/main/scripts/verify.mjs) | Manifest V3・権限が `storage` だけであること・対象サイトが `https://github.com/*` だけであること・参照ファイルの実在と配布一覧との一致・辞書の形式・辞書の語数と README/DESIGN の記載の一致・CSS クラス名の整合・外部通信や `eval` が無いこと |
| 拡張としての動作 | [`tests/e2e/extension.e2e.mjs`](https://github.com/Driedsandwich/repogloss/blob/main/tests/e2e/extension.e2e.mjs)（`npm run test:e2e`） | 配布ファイルだけを並べて Chrome へ実際に読み込ませ、manifest が受理されること・印の表示・コード領域の除外・リンクの中の印が装飾扱いであること・キーボードと Escape・**ON/OFF でページが読み直されず書きかけが残ること**・**タブをまたいだ設定の同期** |

**前の3つが確かめるのは、判定の規則と構成の整合だけ**で、画面上の見え方・速度・読み上げは含まない。4つめの E2E は「拡張として読み込んだ状態」を作るので、`chrome.storage` の実挙動や読み込み順まで含む。

E2E の作り方の要点: `--load-extension` は現行 Chrome で無効化されているため、CDP の `Extensions.loadUnpacked` を使う（`--remote-debugging-pipe` と `--enable-unsafe-extension-debugging` が要る）。`github.com` は `--host-resolver-rules` でローカルの HTTPS サーバへ向けるので、外部通信もアカウントも要らない。**読み込むのは配布する13ファイルだけ**（[`scripts/package-files.mjs`](https://github.com/Driedsandwich/repogloss/blob/main/scripts/package-files.mjs)）なので、出荷物に足りないファイルがあれば落ちる。

### 6-1-2. ブラウザでの実測（2026-08-05）

> **これは当時（v1.7.1 の時点）の補助的な計測方法である。** 拡張として読み込む方法が塞がれていると考えていたため、CDP でページへ content script を流し込み、`chrome.*` を肩代わりして測った。**この方法で分かるのは DOM 上の振る舞いまで。**
>
> **現在は違う。** `Extensions.loadUnpacked` を使えば、配布ファイルをそのまま拡張として読み込んで試験できると分かったので、いまの E2E はすべてその方式で動いている（6-1 を見よ）。この節は、当時どう測ったかの記録として残している。

**速度**（Chrome 151 headless・`k88hudson/git-flight-rules`・3〜5回）

| 版 | 最初の走査 | 走査したテキスト | 付けた印 | レイアウト計算 | 50ms 超の処理 |
|---|---:|---:|---:|---:|---|
| v1.7.1（変更前） | 10〜13 ms | 5,469 か所 | 37 | 526 回 | なし |
| 変更後 | 10〜13 ms | 5,469 か所 | 37 | 526 回 | なし |

**速度は変わっていない**（判定を `matcher.js` へ移しても、走査の量は同じ）。**「大きなページでの速度」を測ったのはこれが初めて**で、結果は 50ms のしきい値に対して十分小さい。DOM 監視のまとめ（バッチ化）を急ぐ理由は、この数字を見る限り無い。

> 測り方の注意: 最初は Node 側で CDP 呼び出しの往復時間を測っており、変更前 51ms・変更後 13ms という差が出た。ページの中で測り直すと両方 10〜13ms で、差は**計測方法の癖**だった。順序を入れ替えても同じ差が出たため、実物の差だと誤認しかけた。

**読み上げ属性**（同じ3ページ・リンクやボタンの中に入った印の数）

| ページ | 変更前：リンク名に解説文が足されていた印 | 変更後 |
|---|---:|---:|
| `octocat/Hello-World` | 19 / 20 | 0 |
| `k88hudson/git-flight-rules` | 32 / 41 | 0 |
| `Spoon-Knife`（コード表示） | 11 / 12 | 0 |

印の総数はどのページでも変更前と同じで、コード表示部分の印は両方とも 0 だった（＝注記の範囲は変えていない）。

### 6-1-3. v1.8.1 での再測定（2026-08-05）

同じ3ページで v1.8.0 と比べた（各3回・中央値）。

| ページ | v1.8.0 | v1.8.1 | 印（前／後） | 文章の中の印 | 50ms 超 |
|---|---:|---:|---:|---:|---|
| `octocat/Hello-World` | 9 ms | 8 ms | 20 / 20 | 0 → **3** | なし |
| `k88hudson/git-flight-rules` | 13 ms | 15 ms | 41 / 41 | 0 → **11** | なし |
| `Spoon-Knife`（コード表示） | 9 ms | 15 ms | 12 / 12 | 0 / 0 | なし |

**注記する語は3ページとも完全に同じ**で、コード表示部分は両方とも 0。変わったのは「印がどこに属するか」で、**本文中の印が自分自身を入口として持つようになった**（v1.8.0 では大きな容器にぶら下がっていた）。

速度はコード表示ページで 9ms → 15ms と目立つが、絶対値は小さく 50ms 超の処理も出ていない。回ごとのばらつきが大きい（10／15／21）ため、この差を確定的なものとして扱わない。

> 途中で 15ms → 29ms へ倍増した時期があり、原因は**入口の判定を一致の有無より先に走らせていた**ことだった（`closest()` を全候補ノードで呼んでいた）。判定を「印を入れると決まってから」に移して戻した。

### 6-1-4. v1.8.3 での再測定（2026-08-06）

**測定条件**: macOS 15 / Google Chrome 151.0.7922.72 の headless / ウィンドウ幅 1600px / 各ページ5回・中央値 / **ページの中で `performance.now()` を使い、初期走査（`scan(document.body)`）だけを測った**（CDP の往復時間を含めない）。v1.8.2 と v1.8.3 候補を、同じ手順・同じ日に測った。

| ページ | v1.8.2 | v1.8.3 | 印（前／後） | 装飾扱い | 語の集合 | 50ms 超 |
|---|---:|---:|---:|---:|---|---|
| `octocat/Hello-World` | 9 ms | 11 ms | 20 / 20 | 17 | **完全一致** | なし |
| `k88hudson/git-flight-rules` | 16 ms | 26 ms | 41 / 41 | 30 | **完全一致** | なし |
| `Spoon-Knife`（コード表示） | 9 ms | 10 ms | 14 / 14 | 12 | **完全一致** | なし |

**注記する語は3ページとも1語も変わっていない**（キーの集合まで突き合わせ済み）。速度は、省いていた描画確認を戻したぶん大きなページで 16→26ms 増えたが、`WeakMap` で測り直しを避けており、しきい値の 50ms には十分収まっている。**正しさを戻すことと引き換えの増加なので、速度を理由に戻さない。**

> ここに書いた数字は、**この条件でのみ**成り立つ。README や過去の節にある「10〜13ms」は v1.7.1 を別の条件で測ったもので、この表とは比べられない（版・幅・回数・計測位置がすべて違う）。版をまたいで数字を並べるときは、条件も一緒に運ぶこと。

### 6-1-5. v1.8.4 での再測定（2026-08-06）

**測定条件**: macOS / Google Chrome 151.0.7922.72 の headless / ウィンドウ幅 1600px / 各ページ5回・中央値 / ページの中で `performance.now()` を使い、初期走査だけを測った。v1.8.3（タグ）と v1.8.4 候補を、同じ手順・同じ日に測った。

| ページ | v1.8.3 | v1.8.4 | 印 | 語の集合 | 見えない領域の中の印 | 50ms 超 |
|---|---:|---:|---:|---|---:|---|
| `octocat/Hello-World` | 13 ms | 17 ms | 20 / 20 | **完全一致** | 1 → **0** | なし |
| `k88hudson/git-flight-rules` | 22 ms | 36 ms | 41 / 41 | **完全一致** | 1 → **0** | なし |
| `Spoon-Knife`（コード表示） | 11 ms | 9 ms | 14 / 14 | **完全一致** | 0 → 0 | なし |

**注記される語は3ページとも1語も変わっていない**（キーの集合まで突き合わせ済み）。増えたぶんは、除外の判定を文字列を読む前へ移したこと（空白だけのノードも `closest(SKIP)` を通る）と、可視性を祖先まで見るようにしたことによる。この測定（開発機）では 50ms に収まっているが、**機械が変わると超える**（6-1-6）。**正しさと引き換えの増加なので、速度を理由に戻さない。**

### 6-1-6. v1.8.5 での測定（2026-08-06・CI の Linux ランナー）

**手元の機械が混んでいてしきい値を示せなかったため、GitHub Actions の ubuntu-latest で測り直した。** 実際の公開ページを開き、v1.8.4 と交互に5往復した（幅1600px・ページの中で初期走査だけを計測）。

| ページ | v1.8.4 中央値 | v1.8.5 中央値 | v1.8.5 最小 | 50ms |
|---|---:|---:|---:|---|
| `k88hudson/git-flight-rules`（用語41件） | 86 ms | **77 ms** | 76 ms | **超過（両方）** |
| `octocat/Hello-World`（用語20件） | 24 ms | **21 ms** | 21 ms | 未満 |

5往復すべてで v1.8.5 のほうが速い。**v1.8.5 は性能の後退ではない。**

**しかし、用語の多いページでは 50ms を超える。v1.8.4 でも超えている**（そちらのほうが遅い）。

> **「50ms 未満」を条件なしで書かない。** この目安は開発機（macOS・空いている状態）の値に基づいていた。CI の ubuntu ランナーでは同じページ・同じ版で倍以上かかる。開発機でも、混んでいる時間帯には v1.8.4 が 61ms、v1.8.5 が 62ms だった。**機械の状態で3倍近く動く量である。** 数字を書くときは、必ずどの機械で測ったかを添える。

### 6-2. 過去の記録

以下は当時その場で書いたスクリプトや実機で確認した記録で、**リポジトリには残っていない**（再実行はできない）。6-1 のテストは、このうち判定に関する部分を再現できる形で書き直したもの。

| 対象 | 方法 | 日付 |
|---|---|---|
| 用語の判定 | 13 ケースの自動テスト（`Pull requests` が `pull` でなく `pull request` と判定される／`repositories` が `repository` と判定される／`force push` が `push` に食われない／`squash merge` が `merge` に食われない／同一文中の複数語がすべて注記される 等） | 2026-08-02 |
| 辞書の自己一致 | 全 61 キーが、それ自身の文字列に対して正しく当たること | 2026-08-02 |
| 検出数の変化 | `github.com/octocat/Hello-World` の表示テキストで 107 → 65 箇所 | 2026-08-02 |
| 構成の整合 | `manifest.json` が参照する 4 ファイルの実在、JSON の妥当性、`node --check`、CSS クラス名の一致 | 2026-08-03 |
| 印の見た目 | ブラウザで描画してスクリーンショットを目視（12px / 16px / 28px とホバー時）。同じ画面で `U+1F6C8` が豆腐になることも再現 | 2026-08-03 |
| 実機での動作 | Chrome で `github.com/octocat/Hello-World` を開き、印31個の表示とツールチップの表示を確認。`Pull requests` に `pull request` の説明が付くことも確認 | 2026-08-03 |
| コード領域の除外 | `github.com/octocat/Spoon-Knife/blob/main/index.html` で、コード表示部分（`.react-code-lines`）の中の印が 0 件であることを確認。現行のコードビューアは `<pre>` `<code>` を使っておらず、効いているのは `.react-code-lines` の指定 | 2026-08-03 |
| 印の密度 | 実機で計測。`Hello-World` 31 個（うち重複9）、`k88hudson/git-flight-rules` 843 個（うち重複800）。同じ語を1回にすると 22 個 / 43 個になる | 2026-08-03 |
| 重複排除のロジック | 5 ケースの自動テスト（同一ノード内の重複／ノードをまたぐ重複／単複が同じ語に潰れる／`repositories` と `repository`／異なる語は両方付く） | 2026-08-03 |
| 重複排除の追随性 | 5 ケースの自動テスト（同一ノード内は1回／別ノードの2回目は付かない／印が消えたら付け直す／付け直した後は再び1回だけ／画面遷移後は付く） | 2026-08-03 |
| 画面遷移での重複 | 4 ケースの自動テスト（ヘッダーに付く／本文では既出の語に付かない／遷移後は消えた語だけ付き直す／ヘッダーが消えたら付き直す） | 2026-08-03 |
| 印の数（直接読み込み） | `git-flight-rules` で 43 個。本文に出現する辞書語 45 のうち、`ci` と `release` はより長いキー（`ci/cd`・`releases`）で既に付いており実質全数 | 2026-08-03 |
| 印の数（画面遷移で到達） | 実機で計測。`git-flight-rules` と Issues 一覧を往復した後で 43 個・重複 0 件。直接読み込みと一致 | 2026-08-03 |
