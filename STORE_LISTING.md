# Chrome ウェブストア 掲載情報（提出記録・公開済み）

作成: 2026-08-03 / 対象: RepoGloss v1.7.1

> このファイルはストアのダッシュボードへ転記するための下書きです。拡張のパッケージには含めても害はありませんが、配布物を小さくしたい場合は ZIP から除いてかまいません。
>
> **2026-08-01 施行のデベロッパープログラムポリシー改定**（Limited Use と開示要件の厳格化）に合わせて書いています。根拠: https://developer.chrome.com/blog/cws-policy-updates-2026

---

## 1. 基本項目

| 欄 | 記入内容 |
|---|---|
| 名前（manifest の `name` が入る） | `RepoGloss — GitHubの英語用語に日本語の解説`（30文字／上限75） |
| 概要（manifest の `description` が入る） | `GitHubの英語UIはそのままに、Git／GitHub固有の用語に印を付け、カーソルを乗せると日本語の解説が出ます。初学者向け。GitHub, Inc.とは無関係の非公式ツールです。`（92文字／上限132） |
| カテゴリ | **仕事効率化（Productivity）** |
| 言語 | **日本語**（主）。説明文は日本語のみで可。英語圏向けの併記は任意 |
| プライバシーポリシー URL | `https://github.com/Driedsandwich/repogloss/blob/main/PRIVACY.md`（2026-08-03 到達確認済み・HTTP 200） |

---

## 2. 詳細説明文

```
GitHub を初めて触ると、fork / rebase / squash merge / upstream といった語で手が止まります。
困るのは英語そのものより、その語が何を指していて、なぜその操作をするのかが分からないことです。

RepoGloss は英語を日本語に置き換えません。英語はそのまま残し、傍らに意味を添えます。
英語の画面に慣れながら読み進められるようにするためです。

■ できること
・ページ上で、Git / GitHub 固有の用語に小さな ⓘ が付きます（同じ語は最初の1回だけ）
・ⓘ にカーソルを乗せると、日本語の説明が出ます
・キーボードだけでも読めます（Tab で ⓘ に止まると説明が出て、Escape で閉じます）
・画面右下のボタンで、いつでも ON / OFF を切り替えられます（ページは読み直しません）

■ 収録している語は 61 語だけです
少ないのは意図的です。「view all」「your profile」のような画面のラベルや、
「on」「open」「code」のような普通の英単語を入れると、無関係な箇所に印が付き、
画面が変わるたびに古くなります。

そこで、画面が変わっても意味が変わらない概念語だけを残しました。
更新が止まっても古い訳を出さない辞書、という設計です。

■ 取り返しのつかない操作は、何を失うかを書いています
・reset … やり方によっては、まだ記録に残していない編集内容が完全に消えること
・revert … 打ち消す記録を後ろに足す方式で、前の記録は消えないこと（reset との違い）
・token … 漏れると、許した範囲の操作を他人が本人として行えること
・ssh key … 秘密の鍵が漏れると、他人が本人の名前でコードを書き換えられること
・force push … 他の人の記録が消えて元に戻せなくなることがあること

■ プライバシー
外部への送信は一切ありません。外部サーバーとの通信を行いません。
Chrome API 権限は storage のみです。画面右下の ON / OFF 設定（真偽値ひとつ）を
端末内に保存するために使います。
サイトアクセスは https://github.com/* に限ります。表示中の文章を、拡張に同梱した
辞書と端末内で照合するためです。読み取った文章は保存も外部送信もしません。
host_permissions、tabs、activeTab、scripting、閲覧履歴への権限は要求しません。

■ 対象
GitHub を使い始めた学生・非エンジニア・文系出身の研究者や実務者。

■ 免責
本拡張は GitHub, Inc. とは無関係で、同社による承認・後援を受けていません。
GitHub は GitHub, Inc. の商標です。

ソースコードは MIT ライセンスで公開しています。誤訳の指摘や語の追加は Issue でお願いします。
```

> **キーワードの入り方**: 「GitHub」「日本語」「用語」「初めて」が本文に含まれています。
>
> 「GitHub」の出現は **6 回** です（実測）。スパムのポリシーが禁じるのは「同一キーワードの**不自然な** 5 回超の反復」と「対応サイト・ブランドを 5 件超**列挙**すること」であり、回数そのものを一律に禁じてはいません。本文の 6 回のうち **3 回は免責条項での商標権者の表示** で、これは減らすと免責の意味が損なわれます。残る 3 回も、GitHub 向けの拡張の説明として自然な言及です。
>
> ただしこれは私の解釈であり、審査側の判断と一致する保証はありません。指摘を受けた場合は、免責以外の言及を減らして再提出してください。

---

## 3. Privacy タブの記入内容

### 3-1. 単一目的（Single purpose）

```
GitHub のページ上に表示されている英語のうち、Git / GitHub 固有の用語に印を付け、
日本語の説明をツールチップで表示すること。これが本拡張の唯一の機能です。
```

> 単一目的の要件に適合します。翻訳・解析・広告・データ収集などの別機能をバンドルしていません。

### 3-2. 権限ごとの正当化

Chrome の権限は、`permissions` に書く **Chrome API 権限**と、コンテンツスクリプトを差し込む**サイトアクセス**に分かれる。`host_permissions` を宣言していないことは事実だが、それだけを書くと「GitHub のページを読むこと」が伝わらないので、必ず両方を書く。

| 区分 | 正当化文（そのまま転記可） |
|---|---|
| Chrome API 権限 `storage` | `画面右下のボタンで切り替える ON / OFF の状態（真偽値ひとつ）を、次回以降も保つために保存します。この用途以外には使用せず、閲覧内容・履歴・個人情報は保存しません。` |
| サイトアクセス `content_scripts.matches` | `対象は https://github.com/* だけです。表示中の文章を、拡張に同梱した辞書と端末内で照合し、一致した用語に印を付けるために読み取ります。読み取った内容は保存も外部送信もしません。` |
| `host_permissions` | **配列は宣言していません。** ただし上記のとおり、コンテンツスクリプトによる GitHub へのサイトアクセスはあります |

> `tabs` / `activeTab` / `scripting` / `<all_urls>` はいずれも要求していません。この点は審査での説明を簡潔にします。

### 3-3. リモートコードの使用

**「いいえ（リモートコードは使用していません）」を選択。**

根拠: 外部スクリプトの読み込み、`eval()` による遠隔文字列の実行、CDN からのライブラリ取得のいずれも行っていません。実行されるコードはパッケージに同梱された **`src/matcher.js` と `src/content.js` の2本だけ**です（`manifest.json` の `content_scripts[0].js` と一致）。

### 3-4. データ収集の申告

**確定しているのは1つだけ**:

- **確定**: 「ウェブサイトのコンテンツ」は選択する（＝収集または使用する）。
- **要確認**: 個人を特定できる情報／認証情報／個人的な通信内容／ウェブ閲覧履歴。
  いずれも「通常の本文に**表示されている**その種の文字列を、辞書照合のあいだ一時的に処理する」
  という一点で答えが変わる。**Developer Dashboard の実画面にある定義文を人が読むまで、
  ここで確定させない。**

v1.8.2 までは全項目を「収集しない」とする方針だったが、これは誤りだった。詳しくは下の注記を参照。

| 項目 | 申告（案） | 根拠 |
|---|---|---|
| **ウェブサイトのコンテンツ** | **選択する** | github.com のページ文章を読み取り、同梱辞書との**照合と可視性の判定に使う**（端末内のみ・保存なし・送信なし）。**CSS で見えなくなっている文章も一時的な対象に含む**。印を付けるのは見えると判定できた一致だけ |
| 個人を特定できる情報 | 選択しない（**要確認**） | 氏名・メール・ID を**個人情報として抽出・識別・保存・送信しない**。ただし画面に出ていれば、表示文章の一部として一時的な照合対象にはなる。定義文が「一時的な処理を含むか」を提出時に確認する |
| 個人的な通信内容 | 選択しない（**要確認**） | 入力欄・`contenteditable`・コード表示・`aria-hidden`・`inert`・`hidden` は、**文字列を取り出す前に**除外する（隔離世界での実測 E2E で確認）。ただし投稿済みのコメントは、ページの文章として一時的な照合対象になる |
| ウェブ閲覧履歴 | 選択しない（**要確認**） | 訪問先を記録・保存・送信しない。`tabs` / `history` 権限も持たない。ただし**画面の切り替わりを検知するため、自分が動いているページの URL を直前の値と比べている**（保存なし・送信なし）。定義文がこの一時利用を含むかを確認する |
| 認証情報 | 選択しない（**要確認**） | **取得元としては触れない**: Cookie を読む API が無く（`cookies` 権限なし）、入力欄・フォーム・編集中の領域は文字列を取り出す前に除外する。**一方、通常の本文に token やパスワードらしき文字列が表示されていれば、ウェブサイトのコンテンツの一部として一時的な照合対象になる**（認証情報として抽出・識別・保存・送信はしない）。定義文が「一時的な処理を含むか」を提出時に確認する |
| 健康情報 / 金融・決済情報 / 位置情報 | 選択しない | 扱わない |
| ユーザーの操作 | 選択しない | クリック等を記録・集計しない |

> **なぜ「収集しない」から改めたか**
>
> Chrome ウェブストアの公式 FAQ は、user data の「handle」に collecting・transmitting・**using**・sharing を含めており、
> ウェブサイトのコンテンツを user data の例として挙げている。そのうえで、
> **端末内だけで処理し外部へ送信しない場合でも、その取り扱いを開示する必要がある**と明記している。
>
> v1.8.2 までは「collect＝利用者のデバイスから転送すること」と解釈して全項目を「収集しない」としていたが、
> この解釈では「使う」が抜け落ちる。実際には本拡張はページ本文を**使って**いるので、開示が要る。
> 過小申告はポリシー違反になりうる（過大申告はならない）。
>
> **提出時にやること**: Developer Dashboard の当該項目に添えられている定義文を実際に読み、
> 「収集する」だけを問うているのか「収集または使用する」を問うているのかを確かめる。
> **外部への転送だけを明確に問う項目である場合に限り**、その定義に従って選択しない。
> 読んだ定義文はそのまま下の欄へ書き写し、判断の根拠を残す。
>
> **「要確認」と書いた4項目**（個人を特定できる情報／認証情報／個人的な通信内容／ウェブ閲覧履歴）は、
> 定義文が「一時的な処理・利用まで含むか」で答えが変わる。**現在の文書だけで
> 「必ず選択しない」と断定しない。** 定義が一時利用まで含むなら、選択する側へ倒す。
>
> 認証情報を「要確認」へ移したのは、v1.8.5 までの根拠（「読む経路が無い」）が
> **取得元の話だけで、通常の本文に出た文字列の一時処理を落としていた**ため。
> 取得元（Cookie・フォーム・編集領域）と、本文として処理し得る内容は別のこととして書く。
>
> **転記欄（項目ごとに1枚ずつ埋める）**
>
> ```text
> 確認日:
> 項目名（画面の表記そのまま）:
> 添えられていた定義文（そのまま書き写す）:
> 選択した内容:
> そう判断した理由:
> コード上の該当箇所:
> 保存:      なし / あり
> 外部送信:  なし / あり
> 第三者共有: なし / あり
> 人手閲覧:  なし / あり
> ```
>
> あわせて、外部送信・保存・共有・人手閲覧をしていない事実は、説明欄で明確に維持する
> （「使う」ことの開示と、「送らない」ことの主張は両立する）。過小申告も、根拠のない
> 過大申告も避ける。

### 3-5. 限定利用（Limited Use）の証明

**チェックを入れる。**

- 開示した単一目的に必要な範囲を超えるデータの利用・転送を行っていません
- そもそも外部への転送を一切行っていません
- 広告・与信・データ販売のいずれにも利用していません

---

## 4. スクリーンショット（撮影済み・4枚）

規格: **1280×800** または 640×400（PNG または JPEG）。最低 1 枚、推奨 5 枚。

撮影する画面の案:

| # | 内容 | ねらい |
|---|---|---|
| 1 | GitHub のリポジトリ画面。ナビゲーションの `Pull requests` `Issues` `Actions` に ⓘ が付いている | 一目で何が起きるか分かる。最重要 |
| 2 | ⓘ にカーソルを乗せ、`pull request` の説明が出ている | 実際の説明文の質を見せる |
| 3 | `reset` または `force push` の説明が出ている | 「危険性まで書いてある」という差別化点 |
| 4 | 画面右下の ON / OFF ボタン | 切り替えられる安心感 |
| 5 | コードを表示している画面で、コード本文に ⓘ が付いていない | 邪魔をしない設計であることの証明 |

**撮影済み**: ローカルの非公開作業フォルダ（掲載用素材置き場）に保管（2026-08-03・すべて 1280×800）

| ファイル | 内容 | 撮影ページ |
|---|---|---|
| `01_nav-markers.png` | ナビゲーションと右サイドバーに印が付いた状態 | `octocat/Hello-World` |
| `02_tooltip-pull-request.png` | `Pull requests` の印にカーソルを乗せ、説明が出ている | 同上 |
| `03_tooltip-danger-reset.png` | `reset` の説明。「記録に残していない編集内容が完全に消え…意味が分かるまで実行しないでください」 | `k88hudson/git-flight-rules` |
| `04_code-excluded.png` | コード表示部分に印が1つも付いていない（ナビゲーションには付く） | `octocat/Spoon-Knife/blob/main/index.html` |

推奨の掲載順は上記のとおり。1枚目で何が起きるかを示し、2枚目で説明の中身、3枚目で危険操作への注意という差別化点、4枚目で邪魔をしないことを示す。

**未撮影**: ON/OFF ボタン単体の寄り（`03` の右下に写ってはいる）。小プロモタイル 440×280（任意）。

小プロモタイル 440×280 も併せて用意すると掲載の見栄えが上がります（任意）。

---

## 5. 提出前チェックリスト

- [x] リポジトリを public にする（2026-08-03 実施）
- [x] `PRIVACY.md` の URL の到達確認（2026-08-03・HTTP 200。旧URLからの 301 リダイレクトも確認）
- [x] 拡張をデベロッパーモードで読み込み、実機で動作確認する（2026-08-03 実施）
- [x] ダッシュボードへの入稿（2026-08-03 完了）。**入稿は全て人間の手作業になる。** Chrome は拡張機能によるウェブストアの操作をブラウザ側で禁じており（`The extensions gallery cannot be scripted.`）、Claude in Chrome からは読み取りもできない。次回のために、フォームの順に並べた入力シート（`INPUT_SHEET.md`）を、ローカルの非公開作業フォルダへ残してある
- [x] スクリーンショットを撮る（2026-08-03・4枚・1280×800）
- [x] ZIP を作る（2026-08-03・`repogloss-1.7.1.zip`・41KB。`.git` と `STORE_LISTING.md` を除外し、展開して参照ファイルの実在と構文を検証済み）
- [x] Privacy タブの各項目の定義文を読み、3-4 の解釈を確認する（本人が実施。申告内容の最終決定は本人）
- [x] 開発者アカウント側の要件（提出が受理されたため充足と判断）
- [x] 「審査に提出」を押す（**2026-08-03 提出完了・審査待ちへ移行**）

---

## 5-2. 審査通過後にやること（完了）

**審査を通過し、公開されました**（掲載を 2026-08-04 に確認。ストアの更新日表示は 2026-08-03 で、提出当日に公開されたとみられます）。

| | |
|---|---|
| 掲載 URL | <https://chromewebstore.google.com/detail/ihkkhkleamggokepaelapoiabgmpljnn> |
| 拡張 ID | `ihkkhkleamggokepaelapoiabgmpljnn` |
| ストア表示 | v1.7.1 ／ 更新日 2026年8月3日 ／ 43.79 KiB ／ 言語 日本語 ／ 評価なし |
| 公開者 | Driedsandwich（`driedsandwichgit@gmail.com`） |

確認方法は、ウェブストアの検索ページと掲載ページを外部から取得し、「Chrome に追加」ボタンの存在を見ています。存在しない拡張 ID を同じ手順に通すと掲載内容が返らないことも併せて確かめました（掲載ページは実在しない ID でも HTTP 200 を返すため、ステータスコードだけでは判定できません）。

なお、掲載ページには開発者の連絡先メールアドレスが表示されます。ストアの仕様で、隠す設定はありません。出ているのは開発者アカウントのアドレス（`driedsandwichgit@`）で、git のコミット著者欄に残っている個人アドレスとは別のものです。

- [x] ストアの掲載 URL を README の「インストール」節へ追記する（2026-08-04）
- [x] 同じ URL を GitHub リポジトリの Website 欄へ設定する（2026-08-04）
- [x] 手動公開の操作は発生していません。通過がそのまま公開になったため、30日の期限は関係ありませんでした

---

## 6. 未解決の論点

1. ~~リポジトリ名が `GitHub-enja-Tooltip` のまま~~ → **2026-08-03 に `Driedsandwich/repogloss` へ改名済み。** 拡張名・リポジトリ名の双方から、商標を先頭に置く形を外した。
2. ~~git 履歴のコミット著者に個人のメールアドレスが残っている~~ → **2026-08-03、案A（既に公開済みのため新たな露出は増えない）で決着し public 化した。** 同じアドレスは他の公開リポジトリのコミットで既に露出しており、この repo の公開で増えるものは無い。→ **2026-08-04 に決着。**今後の露出は GitHub 側の設定で止まっている（メール非公開／露出する push のブロックは、確認したところ以前から有効だった）。**過去のコミット履歴は書き換えない**と判断した。force push しても clone / fork / キャッシュ経由で参照され続けうるため、消える保証がないうえ、書き換えの副作用のほうが大きいという判断による。

3. ~~スクリーンショットの撮影と、それに先立つ実機確認が未了です。~~ → **2026-08-03 に実機確認と撮影を完了。**4枚をストアへ掲載し、うち1枚を切り出して README にも載せています（`docs/screenshot.png`）。
4. ~~審査の所要期間は「多くは数日、最大で数週間」が公式の目安ですが、2026 年 4 月から提出急増による長期化の警告が掲示されたままです。~~ → **今回は提出当日に通った**（08-03 提出、ストアの更新日表示も 08-03）。長期化の警告は掲示されたままでしたが、影響しませんでした。ただし同じ日に出した PostCloak は翌 08-04 の公開だったので、同時に出しても揃うとは限りません。2件だけの実績で、次回の目安にはなりません。

---

## 7. 次にパッケージを作るときの注意

**「何を除くか」ではなく「何を入れるか」で決める。** 配布するファイルの一覧は [`scripts/package-files.mjs`](./scripts/package-files.mjs) にあり、ここが唯一の正本。`scripts/verify.mjs` と拡張として読み込む E2E の両方が同じ一覧を使うので、**足りないファイルがあればテストが落ちる**。

一覧に無いもの（`tests/` `scripts/` `.github/` `package.json` `SECURITY.md` `STORE_LISTING.md` `docs/`）は ZIP に入れない。`docs/` は README 用のスクリーンショット置き場で、拡張の動作には使わない。

---

## 8. 次の提出について

### 8-1. 提出しない版（v1.8.0〜v1.8.10 の11版）

版の順に並べる。**v1.8.0 から v1.8.10 までは1つも提出していない。**

| 版 | タグ | ZIP の SHA-256 | 見送った理由 |
|---|---|---|---|
| v1.8.0 | `f48b24f` | `e0ff2df1…` | 編集できる領域を走査しうる／キーボードで到達できない説明が生じる（再監査で判明） |
| v1.8.1 | `73ab14b` | `379deff2…` | キーボード到達可能性の判定がブラウザの実挙動と一致しない（第3回監査で判明） |
| v1.8.2 | `4212d5c` | `cde7d3fb…` | 描画されない先祖を入口と誤認／矢印で動かない部品を到達可能と誤認／データ申告が公式定義と不整合／Windows E2E が非ゲート（第4回監査で判明） |
| v1.8.3 | `33b1078` | `e76c9245…` | 見えない領域（祖先の `opacity:0`・`content-visibility:hidden`・`inert`・読み上げ専用の1px領域）へ注記し、その語の「最初の1回」を使い切る／除外領域の文字列を除外判定より前に読む／Limited Use 表明が無い／意味の違う普通語（`securities` `cis` 等）を拾う（第5回監査で判明） |
| v1.8.4 | `1ff4829` | `8abc340d…` | 要素自身が中身を隠す形（直接の `content-visibility:hidden`・`hidden="until-found"`）を見抜けない／大きな全面 clip を拾う／退役した印の元の語が二度と注記されない／プライバシーの説明が実装より狭い／検査が提出物の身元ファイルを書き換える（第6回監査で判明） |
| v1.8.5 | `e96c113` | `c8bdbe3d…` | `display:contents` の可視性を先祖の1つの答えで代用（誤りが両方向）／印を片づけるとき隣にあるものを対と推し量る／legacy `clip` を `position` を見ずに判定し読める文章を除外／最低 Chrome 版の未宣言（第7回監査で判明） |
| v1.8.6 | `3848b89` | `6603ca7a…` | 最初の印が消えても既にある2番目の候補へ引き継がない／語と印の対応が壊れても印だけで抑止／切り取りの判定に取りこぼしと落としすぎ／複製された印と入口 ID を自分のものとして扱う／配布物の中でリンクが9件切れている（第8回監査で判明） |
| v1.8.7 | `308bf03` | `9f590f7c…` | 印が不可視・無効になっても退役せず読める同じ語を抑止／語のうしろに文字が増えても整合と誤認／`label` の対応先が変わっても古い入口を保持／保護領域へ移った本文を除外判定より前に読む／所有の判定が class と属性の値に依存しページ側の要素を消す・横取りする／`characterData` を監視しない／面積0の `clip-path` を複数取りこぼす（第9回監査で判明） |
| v1.8.8 | `6c1351a` | `d3a2786b…` | 見張る属性を絞り込んでいたため type・任意の data-*・checked で隠されても気づかない／子を1つ足すだけで祖先が消える形に気づかない／CSS の遷移・画面幅・head の stylesheet を合図にしない／OFF 中の複製が残る／所有だけで「自分の変更」と決めていた／参照ボックス付きの clip-path を解けない（第10回監査で判明） |
| v1.8.9 | `cea7a29` | `4f1f3166…` | 見え方の合図で既にある印を確かめ直すだけで、最初に隠れていた語を永久に見つけない／退役で所有を取り消さず、退役した領域を戻すと印が2個になる／合言葉を書き換えた複製・消した複製が残り、後者は見えない Tab の停止点になる／吹き出しと切替ボタンを class 名で見分けている／半径を省いた `circle()` と参照ボックスだけの `clip-path` を取りこぼす／1回の変更でまとめ直しが2回走る（第11回監査で判明） |
| v1.8.10 | `aaa770d` | `c194a9cb…` | 控えの見直しが、触れてよい場所かを確かめる前に本文を読む／入口がまだ無いだけの語を永久に処理済みにする／見張る前に走査するので最初の外部の文字変更を捨てる／控えの上限で探すのをやめる／参照ボックスが潰れていても inset(0) を可視と扱う／除外一覧とカーソル判定に class 名の衝突が残る（第12回監査で判明） |

| v1.8.11 | `92bd593` | `a3da61d5…` | 語の実際の描画領域と祖先の切り取りを照合せず、画面に1画素も出ていない語へ印を付けて後ろの読める語を抑止／`:hover` だけで短時間ひらく場所の語が説明されない／複製の判定が辞書の説明文に依存し、ページ側の要素を本文ごと消す／ページ側の同名 class へ自分の見た目が乗る／ページが正規の印を外しても約2秒気づかない／退役した印をページが使い回すとその中が走査されない／説明文を書き換えられても正規のまま残る／`<html>` の属性変更を1件も観測していない／控え20,000件で2秒ごとに30〜60msの処理／配布 `DESIGN.md` の既知の限界が現行と食い違う（第13回監査で判明） |

**タグ・Release・履歴・ZIP はすべてそのまま残す**（付け替えも削除もしない）。ZIP は取り違え防止のため、掲載用素材フォルダの `_superseded/` へ置く。v1.8.0〜v1.8.5 の SHA-256 は手元で計算した値で、**リポジトリの CI が検証したものではない**。v1.8.6 以降（`6603ca7a…` / `9f590f7c…` / `d3a2786b…`）は CI の成果物そのものの値。

### 8-2. v1.8.12 の作り方（現在の提出候補）

**提出するのは CI が作った成果物**だけです。手元で作ったものは `--release` 検査で落ちます。

#### 8-2-1. v1.8.12 の提出物（2026-08-11 実測）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.12.zip`（116.18 KiB＝118,968 バイト・13ファイル・展開後 292,315 バイト） |
| **SHA-256** | **`a107c2119ccc1c3b31b32699300d5ea73a1e85549339418ad31d543fb75018bd`** |
| 中身の合算ハッシュ | `99447ee2a2c15c9fc8d96786e2b48596b15d9b0cd063c4ffd72e15d107f9e4bb` |
| 配布13ファイルの合算 | `5e86f647b674ac4336060b6a9e9219e6429882427bf46a44b99b1f4a65a5a268`（ubuntu / macos / windows / 手元 macOS の4者で一致） |
| 出どころ | CI の run [`31468465677`](https://github.com/Driedsandwich/repogloss/actions/runs/31468465677) の `release-zip` ジョブ（artifact `repogloss-store-zip`） |
| タグとの照合 | 展開13ファイルが `git show v1.8.12:<path>` と **13/13 一致**（対照の `v1.8.11` とは5ファイルで相違） |
| `--release` 検査 | 合格（`EXIT=0`）。中身に6バイト足すと `EXIT=1` で落ちることも確認 |

置き場所: `~/dev/repogloss-store-assets/repogloss-1.8.12.zip`（身元の記録は同名の `.json`）。

直前の v1.8.11 の提出候補は `a3da61d5…`（110,325 バイト）で、**第13回監査の指摘により提出しません**。ZIP は `_superseded/` へ退避済み（移動の前後で SHA-256 が変わっていないことを確認）。

#### 8-2-1. v1.8.10 の提出物（2026-08-11 実測）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.10.zip`（105.11 KiB＝107,631 バイト・13ファイル・展開後 256,095 バイト） |
| **SHA-256** | **`c194a9cbabd6496fc97d056b5713fcc2036e258745b43cfae29ecc874ce32fce`** |
| 中身の合算ハッシュ | `3b81332cd60cb4c7de930803a32283c24700794aa159b6ae1af1c0526a25fb23` |
| 配布13ファイルの合算 | `f327e22f9efc91a9ee447a52d6425b70ad8478653a87ff419dd96ea825ddcb5b`（ubuntu / macos / windows / 手元 macOS の4者で一致） |
| 出どころ | CI の run [`31443666381`](https://github.com/Driedsandwich/repogloss/actions/runs/31443666381) の `release-zip` ジョブ（artifact `repogloss-store-zip`） |
| 対象コミット | `aaa770d687ed5861785f629e494662823ba7d875`（タグ `v1.8.10`） |
| 変更点 | `manifest.json`／`src/content.js`／`styles.css`／`README.md`／`DESIGN.md`／`PRIVACY.md` の6ファイル。辞書・`src/matcher.js`・アイコン・`LICENSE` は無変更 |
| 置き場所 | `_superseded/` へ退避済み（第12回監査の指摘で提出しないため） |

**照合済み**: ダウンロードした成果物の SHA-256 が同梱の身元 JSON と一致し（107,631 バイト・13ファイルも一致）、展開した13ファイルがタグ `v1.8.10` と **1バイトも違わない**（対照の `v1.8.9` とは6ファイルで相違。1バイト変えれば検出できることも同じ実行で確認）。`npm run package:verify-zip -- --release` にも合格します（`EXIT=0`）。**この検査に判別力があることも実測しました** — ZIP の `styles.css` に8バイト足すと `EXIT=1`・`NG: styles.css の中身が今のファイルと違う` になります。

**掲載文で差し替えが要る箇所（v1.8.10）**: なし（動作環境・権限・データの扱いは v1.8.9 から変わらない）。

**⚠️ v1.8.9 は提出しません**（第11回監査の指摘）。`repogloss-1.8.9.zip`（`4f1f3166…`）は `_superseded/` へ退避済みです。

### 8-2w. v1.8.9 の作り方（履歴）

#### 8-2-1. v1.8.9 の提出物（2026-08-09 実測）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.9.zip`（99.42 KiB＝101,806 バイト・13ファイル・展開後 237,232 バイト） |
| **SHA-256** | **`4f1f3166c8ee35b76786f6062307a96a6ea25772fc0d51dfdb06a9623d70112f`** |
| 中身の合算ハッシュ | `8526da7beaf15ef63c0bcfd95f8b93cb563181d772af5f441966d41346f4b23b` |
| 配布13ファイルの合算 | `1979bbe2a560d5037f233767adb2b6a3dc7cb8d0d93e8234447ac39970a01609`（ubuntu / macos / windows / 手元 macOS の4者で一致） |
| 出どころ | CI の run [`31318907971`](https://github.com/Driedsandwich/repogloss/actions/runs/31318907971) の `release-zip` ジョブ（artifact `repogloss-store-zip`） |
| 対象コミット | `cea7a29590747f110d6cda68c162d339499c5fdf`（タグ `v1.8.9`） |
| 変更点 | `manifest.json`／`src/content.js`／`styles.css`／`README.md`／`DESIGN.md`／`PRIVACY.md` の6ファイル。辞書・`src/matcher.js`・アイコン・`LICENSE` は無変更 |
| 置き場所 | `_superseded/` へ退避済み（第11回監査の指摘で提出しないため） |

**照合済み**: ダウンロードした成果物の SHA-256 が同梱の身元 JSON と一致し（101,806 バイト・13ファイルも一致）、展開した13ファイルがタグ `v1.8.9` と **1バイトも違わない**（対照の `v1.8.8` とは6ファイルで相違。1バイト変えれば検出できることも同じ実行で確認）。`npm run package:verify-zip -- --release` にも合格します（`EXIT=0`）。**この検査に判別力があることも実測しました** — ZIP の `styles.css` に8バイト足すと `EXIT=1`・`NG: 1 件` になります。

**掲載文で差し替えが要る箇所（v1.8.9）**: なし（動作環境・権限・データの扱いは v1.8.8 から変わらない）。

**⚠️ v1.8.8 は提出しません**（第10回監査の指摘）。`repogloss-1.8.8.zip`（`d3a2786b…`）は `_superseded/` へ退避済みです。

### 8-2x. v1.8.8 の作り方（履歴）

#### 8-2-1. v1.8.8 の提出物（2026-08-08 実測）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.8.zip`（95.28 KiB＝97,563 バイト・13ファイル・展開後 223,803 バイト） |
| **SHA-256** | **`d3a2786ba0f87ac51a75bcb6e9186a6ae3e86ea93ef804905da7fefebcf9bfac`** |
| 中身の合算ハッシュ | `6b900e94ca8d1d27742298c5f7e3b64e5c993a7dfa14ff54f25897752e0ddd49` |
| 配布13ファイルの合算 | `7524616ead29a77ef876d1f6660222d88af93bcfec9580b42d4682fc77c16e2b`（ubuntu / macos / windows / 手元 macOS の4者で一致） |
| 出どころ | CI の run [`31245180346`](https://github.com/Driedsandwich/repogloss/actions/runs/31245180346) の `release-zip` ジョブ（artifact `repogloss-store-zip`） |
| 対象コミット | `6c1351a5b98539a61e854310503fd92ad59a7d24`（タグ `v1.8.8`） |
| 変更点 | `manifest.json`／`src/content.js`／`README.md`／`DESIGN.md`／`PRIVACY.md` の5ファイル。辞書・`src/matcher.js`・`styles.css`・アイコン・`LICENSE` は無変更 |
| 置き場所 | `_superseded/` へ退避済み（第10回監査の指摘で提出しないため） |

**照合済み**: ダウンロードした成果物の SHA-256 が同梱の身元 JSON と一致し、展開した13ファイルがタグ `v1.8.8` と **1バイトも違わない**（対照の `v1.8.7` とは5ファイルで相違）。`npm run package:verify-zip -- --release` にも合格する（`EXIT=0`）。**この検査に判別力があることも実測しました** — ローカルの HEAD がタグと違う状態では `EXIT=1` になります。

**掲載文で差し替えが要る箇所（v1.8.8）**: なし（動作環境・権限・データの扱いは v1.8.7 から変わらない）。

**⚠️ v1.8.7 は提出しません**（第9回監査の指摘）。`repogloss-1.8.7.zip`（`9f590f7c…`）は `_superseded/` へ退避済みです。

### 8-2y. v1.8.7 の作り方（履歴）

#### 8-2-1. v1.8.7 の提出物（2026-08-08 実測）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.7.zip`（88.09 KiB＝90,204 バイト・13ファイル・展開後 199,883 バイト） |
| **SHA-256** | **`9f590f7c299d8c5f4bd62fc9cf87a5a9a2c8215945683faca76d0cf496afb93e`** |
| 中身の合算ハッシュ | `461e428d157baa8f818d4ea9115714d32f0080f7124aa57c1e17a95630f7973e` |
| 配布13ファイルの合算 | `1d9bcf5193ada70577a41a4dd5f2c15c71cbe16ade3b7f1397071debb719125f`（ubuntu / macos / windows / 手元 macOS の4者で一致） |
| 出どころ | CI の run [`31230981587`](https://github.com/Driedsandwich/repogloss/actions/runs/31230981587) の `release-zip` ジョブ（artifact `repogloss-store-zip`） |
| 対象コミット | `308bf038efe18f857bf5bb655bce65b991712a93`（タグ `v1.8.7`） |
| 変更点 | `manifest.json`／`src/content.js`／`README.md`／`DESIGN.md`／`PRIVACY.md` の5ファイル。辞書・`src/matcher.js`・`styles.css`・アイコン・`LICENSE` は無変更 |
| 置き場所 | `_superseded/` へ退避済み（第9回監査の指摘で提出しないため） |

**照合済み**: ダウンロードした成果物の SHA-256 が同梱の身元 JSON と一致し、展開した13ファイルがタグ `v1.8.7` と **1バイトも違わない**（対照の `v1.8.6` とは5ファイルで相違）。`npm run package:verify-zip -- --release` にも合格する（`EXIT=0`）。**この検査に判別力があることも実測しました** — ローカルの HEAD がタグと違う状態では「出どころのコミットが、いま checkout している内容と違う」で `EXIT=1` になります。

**掲載文で差し替えが要る箇所（v1.8.7）**: なし（動作環境・権限・データの扱いは v1.8.6 から変わらない）。

**⚠️ v1.8.6 は提出しません**（第8回監査の指摘）。`repogloss-1.8.6.zip`（`6603ca7a…`）は `_superseded/` へ退避済みです。

### 8-2z. v1.8.6 の作り方（履歴）

作り方は v1.8.5 と同じで、**提出するのは CI が作った成果物**だけです（手元で別に作ると、作ったものと検査したものが別になりうる）。

```sh
npm test && npm run test:e2e          # 検証がすべて通ることを先に確認する
npm run package:stage                 # dist/repogloss-1.8.6/ へ配布ファイルだけを並べる
npm run package:verify                # 並べたものが今の中身と一致するか確かめる
npm run package:zip                   # ZIP を作り、開き直して中身を検査する
npm run package:verify-zip -- --release   # 提出候補として、身元の記録まで検査する
```

`--release` を付けると、身元の記録が「CI の、この repository の、`main` への push から出たもの」であることまで見る（v1.8.6 で追加）。手元で作ったものはここで落ちる。CI の `release-zip` はこのモードで検査している。

#### 8-2-2. v1.8.6 の提出物（2026-08-06 実測）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.6.zip`（84.10 KiB＝86,123 バイト・13ファイル） |
| **SHA-256** | **`6603ca7a2f7653f3786b197b4058bfc45c354a8c5692dcdd5e3cf2455018ba35`** |
| 中身の合算ハッシュ | `18464c772767c5d04c1bb484ffe8ae1d774de1ca5d74bd1ec131b5a086252709` |
| 出どころ | CI の run [`31110318795`](https://github.com/Driedsandwich/repogloss/actions/runs/31110318795) の `release-zip` ジョブ（artifact `repogloss-store-zip`） |
| 対象コミット | `3848b89a9cdc7df62dc2eaff8c4ae4b6e6e18086`（タグ `v1.8.6`） |
| 置き場所 | 掲載用素材フォルダの直下（`repogloss-1.8.5.zip` は `_superseded/` へ退避済み） |
| 変更点 | `manifest.json`（version・`minimum_chrome_version`）／`src/content.js`／`README.md`／`DESIGN.md`／`PRIVACY.md` の5ファイル。辞書・`src/matcher.js`・`styles.css`・アイコン・`LICENSE` は無変更 |

**照合済み**: ダウンロードした成果物の SHA-256 が CI のログと一致し、展開した13ファイルがタグ `v1.8.6` と1バイトも違わない（対照の `v1.8.5` とは5ファイルで相違）。別の機械で `--release` 検査にも合格する。手元（macOS）と CI（Linux）で ZIP の SHA-256 が完全に一致した。

**⚠️ 提出は外部監査に合格してから。** この時点では入稿していない。

**掲載文で差し替えが要る箇所（v1.8.6）**: 動作環境に「Chrome 105 以降」を書く。データ申告は §3-4 のとおり、確定1件・要確認4件のまま提出時に人が判断する。

### 8-2b. v1.8.5 の作り方（履歴）

ZIP の作成と検査を CI に移した。**提出するのは CI が作った成果物**で、手元で別に作ったものは使わない（作ったものと検査したものが別になりうるため）。

```sh
npm test && npm run test:e2e     # 検証がすべて通ることを先に確認する
npm run package:stage            # dist/repogloss-1.8.5/ へ配布ファイルだけを並べる
npm run package:verify           # 並べたものが今の中身と一致するか確かめる
npm run package:zip              # ZIP を作り、開き直して中身を検査する
npm run package:verify-zip       # できた ZIP をもう一度だけ検査する
```

`npm run package:zip` は、配布対象に commit していない変更があると **止まる**（警告ではなく終了コード 1）。試すだけなら `-- --allow-uncommitted` を付ける。その場合、ファイル名に `UNCOMMITTED` が入るので提出物と取り違えない。

検査で見ているもの: 配布一覧との過不足0 ／ 1件ずつの中身が手元と1バイトも違わない ／ ZIP の中の version が一致 ／ 余分な親フォルダで包まれていない ／ 開発用・鍵らしきファイルの混入なし ／ 末尾に足されたごみが無い。**検査が働いていることの対照**は `tests/package.test.js` にあり、壊した ZIP 9 種類を1件ずつぶつけて必ず落ちることを確かめている（13件のうち9件が「壊れたものを落とす」試験、残り4件が対照）。

#### 8-2-2. v1.8.5 の提出物（2026-08-06 実測）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.5.zip`（78.13 KiB＝80,004 バイト・13ファイル） |
| 出どころ | **main の CI が作った成果物 `repogloss-store-zip`**（run `31094587675` の `release-zip` ジョブ）をダウンロードしたもの。手元でビルドしたものは使っていない |
| SHA-256 | `c8bdbe3d5c140b68525d3d9a851647edec7384a0449aa6d6903c5400e15d9d29` |
| 中身の合算ハッシュ | `4e07719134aa4395693b9b57f81ec93a45368e15de92c2153a75cb124d804ec0` |
| 対応するタグ | `v1.8.5` = コミット `e96c113`（PR #15 の merge commit）。`main` の先端も同じコミット |
| 置き場所 | 掲載用素材フォルダ直下（提出しない版は `_superseded/` へ退避済み） |

**照合の結果**: ダウンロードして計算した SHA-256 が CI のログと一致した。展開した13ファイルは**タグ `v1.8.5` の中身と1バイトも違わない**。**対照**として v1.8.4 と比べると5ファイルで相違が出るので、比較そのものが働いていることも確かめてある。

**CI の検証結果**（run `31094587675` / headSha `e96c113`）: **8ジョブすべて success**。

#### 8-2-2-b. v1.8.4 の提出物（2026-08-06 実測・**提出しない**）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.4.zip`（74.20 KiB＝75,980 バイト・13ファイル） |
| 出どころ | **main の CI が作った成果物 `repogloss-store-zip`**（run `31076712703` の `release-zip` ジョブ）をダウンロードしたもの。手元でビルドしたものは使っていない |
| SHA-256 | `8abc340d23e352bf2c14972f67cb0c17113ec414de5255a6b786b51153d0fdda` |
| 中身の合算ハッシュ | `acc967366d0120fe9643a80b5aaae5386cf75944de53435dbf272b46653347a3` |
| 対応するタグ | `v1.8.4` = コミット `1ff4829`（PR #14 の merge commit）。`main` の先端も同じコミット |
| 配布物の合算ハッシュ | 3 OS で一致することを `hash-compare` ジョブが機械的に確認（値が割れると CI が落ちる） |
| 置き場所 | 掲載用素材フォルダ直下（提出しない版は `_superseded/` へ退避済み） |

**照合の結果**: CI の成果物をダウンロードして計算した SHA-256 が、CI のログの値と一致した。展開した13ファイルは**タグ `v1.8.4` の中身と1バイトも違わない**（`git show v1.8.4:<path>` と1件ずつ突合・Python の `zipfile` で読んだ）。**対照**として v1.8.3 と比べると7ファイルで相違が出るので、比較そのものが働いていることも確かめてある。

**別環境でも同じものができた**: 手元（macOS・Node 22.22.3）と CI（Linux・Node 22.23.1）で **ZIP の SHA-256 が一致**した。zlib はどちらも 1.3.1。Node の patch 版が違っても同じになることを実測できた（zlib の版が変わった場合は未確認）。

**CI の検証結果**（run `31076712703` / headSha `1ff4829`）: **8ジョブすべて success**。

> **提出候補の成果物ゲートが働くことの確認（2026-08-06）**: 使い捨てブランチで `release-zip` の対象ブランチだけを一時的に付け替え、**同じブランチで対照→破壊の順**に測った。無傷なら `release-zip` は success で成果物が出る。`e2e-windows` をわざと落とすと **`release-zip` は skipped** になり、`repogloss-store-zip` は作られなかった（run 全体も failure）。また **PR では `release-zip` が skipped** になり、成果物は `package-hash-*` の3件だけになる——変更前の PR #11 では `repogloss-store-zip` が出ていたので、実際に塞がったことを前後で確認できる。確認後、検証用ブランチはローカル・リモート両方から削除した。

#### 8-2-2-b. v1.8.3 の提出物（2026-08-06 実測・**提出しない**）

| 項目 | 値 |
|---|---|
| ZIP | `repogloss-1.8.3.zip`（67.61 KiB＝69,230 バイト・13ファイル） |
| 出どころ | **main の CI が作った成果物 `repogloss-store-zip`**（run `31067455917` の `release-zip` ジョブ）をダウンロードしたもの。手元でビルドしたものは使っていない |
| SHA-256 | `e76c924522339941fd6397c66302446d13046e92f0687a26d1ca068ba338f4fa` |
| 中身の合算ハッシュ | `66a08bea5e89dbfca77c0998757630565bffe06deee8d55dd704abbb7c348c3c` |
| 対応するタグ | `v1.8.3` = コミット `33b1078`（PR #11 の merge commit）。`main` の先端も同じコミット |
| 配布物の合算ハッシュ | `f076ac6de81e53c4d6d6656fa927babfd5cb0433e92d46ec202d75564d575245`。**Windows / Ubuntu / macOS の3つとも同じ値**であることを `hash-compare` ジョブが機械的に確認（値が割れると CI が落ちる） |
| 置き場所 | 掲載用素材フォルダ直下（提出しない版は `_superseded/` へ退避済み） |

**照合の結果**: CI の成果物をダウンロードして計算した SHA-256 が、CI のログに出た値と一致した。展開した13ファイルは**タグ `v1.8.3` の中身と1バイトも違わない**（`git show v1.8.3:<path>` と1件ずつ突合）。配布一覧との過不足なし、開発用ファイルの混入なし。

**比較そのものが働いていることの対照**: 手元で同じコミットからビルドした ZIP も同一（`e76c9245…`）で、**別のマシン・別の OS でビルドしても同じものができる**ことを確かめた。一方、1文字変えた期待値とは一致せず、v1.8.2 の ZIP（`cde7d3fb…`）とも相違が出る。中身は Python の `zipfile`（この実装とは無関係）でも 13件・破損なし・version 1.8.3 と読める。

**CI の検証結果**（run `31067455917` / headSha `33b1078`）: **8ジョブすべて success**。`e2e-windows` も実際に走っている（`found: C:\Program Files\...\chrome.exe` → テスト 32件すべて成功）。

> **Windows のゲートが働くことの確認（2026-08-06）**: `continue-on-error` を外したことが実際に効くかを、使い捨ての PR で確かめた。Windows でだけ落ちる assertion を1つ入れると、`e2e-windows` が赤・他7ジョブは緑・run 全体の結論は `failure` になった。対照として無傷の PR #11 は `success`。確認後、その PR は閉じてブランチをローカル・リモート両方から削除した。

### 8-2-3. 提出の前提（2026-08-05 決定）

**⚠️ v1.8.4 は提出しない**（第6回監査で P1 3件・P2 4件）。下のチェックは v1.8.5 に対して取り直したもの。

**外部監査に合格してから提出する。** 1〜4巡目とも、提出直前の版に対して指摘が出た（15件・12件・6件・8件、いずれも全件が実在）。合格を確認せずに出すと、直せたはずの欠陥を載せたまま公開することになる。

- [ ] 最新版に対する外部監査を受け、**指摘が0件（または P1／P2 が0件）である**ことを確認した
- [x] `npm test` / `npm run test:e2e` / `package:verify` / `package:verify-zip` がすべて成功（2026-08-06）
- [x] GitHub Actions の8ジョブすべて成功（main の run `31094587675`・2026-08-06）
- [x] 提出する ZIP が **CI の成果物そのもの**であり、SHA-256 を控えた（`c8bdbe3d…`）
- [ ] Privacy タブの「ウェブサイトのコンテンツ」について、§3-4 の欄に定義文を書き写して判断を記録した

### 8-3. 審査に関わる差分（v1.7.1 → v1.8.7）

**権限は増えていない**（Chrome API 権限は `storage` のみ）。サイトアクセスはむしろ **狭めた**（HTTP も含む指定をやめ、`https://github.com/*` だけにした）。読み取り・保存・送信の実際の動きも変わっていない。

**ただしデータ申告は変える。** 動作が変わったからではなく、**これまでの申告が公式定義と合っていなかった**ため（§3-4）。「ウェブサイトのコンテンツ」を選択する側へ改める。

**動作環境**: v1.8.6 から `minimum_chrome_version: "105"` を宣言する。これより古い Chrome の利用者には配信されない。

**掲載文で差し替えが要る箇所**: §2 の「■ できること」（キーボードで読めること、ON/OFF でページを読み直さないこと）と「■ プライバシー」（API 権限とサイトアクセスを分けた文、および「端末内で使う」ことの明示）。概要（`description`）とスクリーンショットは前回のままでよい。
