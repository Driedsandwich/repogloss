# v1.8.14 の変更 — 第15回監査（P1 4件・P2 5件）への対応

対象: `v1.8.13`（コミット `54f98991…`）／基点 `50026b45934e79406fb8c7abbf17e5d4946568a4`

**9件のうち7件を直しました。2件は直していません**（→ 最後の節）。直した7件はすべて、直す前に v1.8.13 の実物で再現しています。

## P1 4件

| ID | 再現（v1.8.13・前方/後方の印） | 直し方 |
|---|---|---|
| RG-15-01 | 同じ節点に `branch` が2つあり1つ目だけ切り取られている → **0 / 1**（読める2つ目に付かない） | 見え方で絞ってから、キーを1つにする |
| RG-15-02 | `inset(0 round 0 60px 0 0)` の**丸めた角**の語 → **1 / 0** | 1〜4値と `/` を角ごとの `rx`/`ry` へ展開 |
| RG-15-03 | 2つの円で別々の断片が別々の形を通る → **1 / 0** | **同じ断片が全部の条件を通る**ことを要求 |
| RG-15-04 | `position:fixed; left:-10000px` の語 → **1 / 0**（`scrollX` は 0 のまま動かせない） | 画面／スクロールで出せる範囲との交差を要求 |

直したあと、4件とも期待どおり（1つ目は 1/0、残りは 0/1）になりました。

**⚠️ RG-15-04 の直しで、入れ子のスクロール領域の中身を巻き込みました。** 既存の対照（`overflow:auto` の画面外）が落ちて気づき、**スクロールできる入れ物の中は範囲の検査をしない**ようにしています。監査の指示書にも同じ注意が書かれていました。

## P2 3件（直したもの）

| ID | 再現 | 直し方 |
|---|---|---|
| RG-15-05（前半） | `color: transparent` の語に印1 | 文字色と `-webkit-text-fill-color` が透明で縁取りが無ければ不可視 |
| RG-15-07 | 150ms 以内の2回目を**捨てて**いた | 窓が明けたあとに最後の1回を必ず処理する |
| RG-15-08 | 名札を全部消された複製が幅0・`tabindex=0` で残る | 自分が書く読み上げ名（`「…」の解説`）でも見分ける |

## 直していない2件

- **RG-15-06（カスケードレイヤーの名前衝突）**: ページ側が `@layer page` を使う場合の取りこぼし、`@layer repogloss-scope, repogloss` を先に宣言された場合の順序反転、`iiyaku-off` class の衝突。**レイヤー名と class 名を読み込みごとに変える設計変更**が要り、この版では手を付けていません。いまの実装では、ページがこれらの名前を意図的に使うと、見た目の絞り込みが効かなくなります。
- **RG-15-05 の後半（`filter` の並び順）**: `opacity(0)` を list 内の位置に関係なく全面非表示と断定しています。後段の SVG filter が独自の画像を作る場合を見抜けません（落としすぎの方向）。

## 検証

| 何を | 結果 |
|---|---|
| 単体テスト | 63件 全成功 |
| 構成と文書の整合 | 284項目・不一致 0 |
| E2E | 206件 全成功 |

**反例は E2E へ入れました**（206 → **218件**）。新しい10件を v1.8.13 の実装へぶつけると **8件が失敗**します（通る2件は落としすぎを見張る対照＝丸めていない角の語・スクロールで出せる入れ物の中）。
